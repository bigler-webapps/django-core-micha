# WORK_ORDERS.md — django-core-micha

Work-order register for this repo. Lightweight directory (not the full orders):
one row per WO with its implementation status. Convention, schema, and maintenance
rules are defined centrally in `webapps/AGENTS.md` → "Work-Order Register".

**Scope note:** this register starts with the 2026-07 resilience workstream;
older work is in `git log`.

## Workstream prefixes

| Prefix | Workstream |
|---|---|
| `RES-*` | Runtime/network resilience (healthz bounding, retry on external calls) |
| `NOTIF-*` | Shared notifications platform (canonical model, router, category×channel prefs, todo channel) |
| `MSG-*` | Shared messaging platform, Phase B (generic scope, policy hooks, encryption-at-rest, ucm chat surfaces; jg-ferien first consumer via MSG-5, spesix deferred to MSG-4 per the 2026-07-31 re-ordering) |
| `SEC-*` | Secrets hygiene (`sync-secrets` destination matrix, `.env`/`.env.local` handling) |
| `DX-*` | Developer/test environment ergonomics (test-DB isolation, local run patterns). **Repo-local:** `ui-core-micha` has its own unrelated `DX-*` series (dev harness), so always qualify the repo when referencing a `DX-n` across repos. |

Introduce a new prefix when none fits and add it here. New WOs always get a
prefixed ID; never reuse a bare flat number across workstreams.

## Register

| ID | Titel | Beschreibung | Datum | Status | Commit(s) | Notiz |
|---|---|---|---|---|---|---|
| RES-1 | Bounded healthz + sync-secrets push retry | Per-check wall-clock timeout in `/api/healthz` (fast 503 instead of gateway 504 on dependency stalls); Redis socket timeouts + Postgres connect_timeout + TCP keepalives in settings_base; retry + failure aggregation (non-zero exit) around the `gh secret set` push loop | 2026-07-16 | done | 3e8f01f | v2.25.0 → 2.26.0 (publish-from-main). Reviewed independently (2 rounds): round 1 confirmed a real gap — per-request `ThreadPoolExecutor` workers are non-daemon and get joined by `concurrent.futures.thread`'s process-wide shutdown hook regardless of `shutdown(wait=False, ...)`, so a sustained outage leaks threads that block graceful restarts (contradicted the original "no accumulation" claim). Resolved per operator decision: kept the design (no shared pool, no daemon threads), added Postgres TCP keepalives (~60s dead-peer detection) to bound leaked-thread lifetime, corrected risk wording in views.py docstring + CHANGELOG. Round 2: no findings. App pin bumps explicitly OUT of scope (per-app operator decision; every develop push = staging deploy). CHANNEL_LAYERS untouched (pubsub-layer history); no app-wide statement_timeout |
| NOTIF-1 | Canonical Notification models | Concrete `Notification` + `NotificationRecipient` + `NotificationDelivery` in dcm (dedup_key first-class, notifiable GenericFK indexed, retention fields); additive new tables, existing consumers untouched `[approval schema]` | 2026-07-18 | done | 1044f70 | D3 ratified 2026-07-18 (swappable exit). Codex-built, independent `reviewer` (R1-R5); R1-R4 fixed (for_concrete_model consistency, imports, redundant index, +2 tests), orchestrator review clean, 305 passing. Additive-only migration `0002`, NOT wired, version unbumped (2.26.0) — accumulates to 2.27.0 publish at end of dcm P1 slice (NOTIF-4). Full spec: `docs/design/notifications-platform.md` |
| NOTIF-2 | Router + notify() + type registry | Router (D2 precedence `eligible ∩ (override ?? default) ∩ prefs` + `force` for critical) + `notify()` authoring API + code-first type-registry loader | 2026-07-18 | done | 1e7a0dd | Codex-built, independent `reviewer` (R1-R4). R1 (P1, real bug): `_send_email`/`_send_push` re-filtered by opt-in internally, silently defeating the router's critical-force path while `dispatch()` still reported "sent" — fixed via a backward-compatible `bypass_preference_check=False` param on both (delivery.py touch, deviates from the WO's own DO-NOT-TOUCH, operator-approved). R3 (gettext("") catalog-header fallback) and R4 (bare `assert` stripped under `-O`) fixed. R2 (P2, carried forward): `NotificationDelivery` unique_together over nullable `digest_threshold` means SQL NULL≠NULL — same race pattern as NOTIF-1 R5 one level down; needs a NOTIF-1 schema change, out of scope here, revisit at NOTIF-4/7. NOTIF-1 R5 (dedup IntegrityError retry) implemented in `api.py`. 315 passing. Version unbumped (2.26.0) |
| NOTIF-3 | Category×channel prefs | `NotificationChannelDefault` (per-channel default) + `NotificationCategoryChannelPreference` (per-category×channel override); rebacks `prefs.py::is_channel_enabled` behind the same NOTIF-2 signature `[approval schema]` | 2026-07-18 | done | c1797a0 | Codex-built, independent `reviewer` (R1-R2). R1 (P1, real bug): the WO's originally-specified one-time seed migration (backfilling `NotificationChannelDefault` from every existing `NotificationPreference` at migration time) would permanently freeze pre-existing users' behaviour, silently defeating any later toggle via the still-live `NotificationPreferenceView` endpoint — operator decided (AskUserQuestion) to drop the seed migration entirely instead of inverting precedence or accepting the risk; existing users now fall through to a live `NotificationPreference` read (tier 3) indefinitely until a future Prefs-UI explicitly sets a channel default (tier 2), which permanently and correctly preserves today's behaviour with no staleness window. R2 (docstring omitted tier 3) fixed — `is_channel_enabled` now documents all 4 tiers. Migration `0003` is additive-only (CreateModel×2, no RunPython). Legacy `email_opt_in`/`push_opt_in` columns retained. 317 passing. Version unbumped (2.26.0) |
| NOTIF-4 | Dispatchers + retention janitor + publish 2.27.0 | Formal `Dispatcher` protocol + registry + bounded retry hook; R2 fix (`NotificationDelivery` partial unique constraints for the nullable `digest_threshold` race + `_get_delivery_with_retry`); `prune_notifications` retention command (90d default, orphan sweep deferred); version bump 2.26.0→2.27.0 | 2026-07-18 | done | b7c97d6 | Codex-built (nested `codex exec` self-delegation observed mid-run — verified both bypass flags present, no stall), independent `reviewer` (R1-R2, both non-blocking). R1 (P2): CHANGELOG originally overclaimed "retryable delivery outcomes are retried" — no dispatcher shipped today (chip/email/push) can ever report a transient failure (delivery.py swallows per-recipient exceptions internally), so retry is currently dead scaffolding for a future dispatcher — CHANGELOG wording softened to say so explicitly. R2 (P3): `dispatch()`'s retry-skip condition is correct but the `ok=None` (pending stub) case relies on `None is not False`; added a clarifying comment. Migration additive/constraint-only (touches only `NotificationDelivery`). 324 passing. **This WO's push is the 2.27.0 PyPI publish** — consumers pick it up only on pin-bump (NOTIF-5 ucm, NOTIF-6 cockpit) |
| NOTIF-5 | Canonical read API + WS status broadcast | dcm serializer for `NotificationRecipient`+content; **routes are `feed/`, `feed/unread-count/`, `feed/mark/`** (NOT `inbox/*` — that's the still-live legacy swappable-model path, unavailable to reuse until NOTIF-7 retires it); unread-count = `seen_at IS NULL AND dismissed_at IS NULL`; mark = seen/dismissed/done (IDOR-safe, self-scoped); WS status-change broadcast (`notification.status`, one per distinct notification, full post-update tri-state) so a mark on one surface clears others live. **No migration** (recipient status fields exist) | 2026-07-18 | done | abe576d | Codex-built direct (no nesting this time, per explicit instruction), independent `reviewer`: no P1 findings, clean IDOR/content-leak review (self-scoping verified on both read and mark, `notifiable` never dereferences the target). R1 (P2, docs-only): design doc + this row originally said `inbox/*` for the new routes — the implementation correctly shipped `feed/*` instead since `inbox/*` was taken by the legacy views, but the docs weren't reconciled; fixed here + in `docs/design/notifications-platform.md`. 338 passing. **This WO's push is the 2.28.0 PyPI publish** — NOTIF-6 (ucm) must target `feed/*`, not `inbox/*` |
| NOTIF-6 | ucm context + chip/bell | `NotificationsProvider`/`useNotifications` single-owner (WS+feed+unread) reading the NOTIF-5 canonical `feed/*` API + generic `NotificationBell`; additive exports (no breaking change) (ui-core-micha) | 2026-07-27 | done | 6c63fb9 | Codex-built direct (no nesting), independent `reviewer`: single-WS-owner invariant traced clean. R1 (P2, real bug): the `notification.status` WS handler patched item flags but never adjusted `unreadCount`, leaving the badge stale after a cross-tab/cross-device mark — fixed (delta computed from before/after unread state, mirroring `mark()`'s own logic) + regression test added. 57 passing. Published ucm 2.11.0 |
| NOTIF-7 | cockpit swappable-exit + cutover | Staged (one feature, implementer-chunked, 4 sub-stages): pin dcm 2.28.0/ucm 2.11.0 (+regression across the 5-minor gap: settings_base 2.26 timeouts/healthz vs cockpit custom CHANNEL_LAYERS, ucm 2.10 browserPush default) → remodel status-stream (`notify/services.py` + `status/celery_tasks.py`) to canonical `notify()` (i18n keys + `notifiable` MonitorState/WorkflowState/RepoState + recovery resolver=state-resolved) → frontend cutover `Header`/`App` → ucm bell/context on `feed/*` → retire old inbox + `NOTIFICATION_MODEL`. **NO historical data migration** (transient status rows not relevant per operator 2026-07-18, discarded at retirement; content-mismatch shim moot) `[approval schema]` | 2026-07-27 | done | 8119695 (s1), c08db3b (s2), bc0c8a7 (s3), 9f84bec (s4) | cockpit repo (own register: cockpit `WORK_ORDERS.md`). Each stage independently reviewed. Stage 2's `reviewer` found a real gap (`resolve_status_problem` lacked exception isolation, risking an uncaught DB error aborting a poll cycle) — fixed + tested; recovery-resolver join-key (specific state row, not shared `StatusEntity`) verified via a real DB-level two-sibling-monitor isolation test. Stages 3/4: clean reviews, no findings. Landed on cockpit `develop` (staging); `develop→main` prod promotion is a separate operator-gated step, not part of this WO. **Flagged separately, not blocking:** pre-existing unrelated pending migrations in cockpit's `ingest`/`kb`/`tasks`/`users` apps (confirmed present even without this WO's changes) — operator awareness item. **Follow-up (separate, future, approval-gated):** drop the now-dormant `notify_notification` table/app. Closes P1 |
| NOTIF-P2-pre | jg | Normalize `build_checklist_tasks` onto config/materialize path; collapse triplicated `leadAdjustable` set to one source; audit/clean `profile_complete` orphan rows | 2026-07-27 | done | jg repo: a1eaac9 | Codex-built direct, independent `reviewer` clean. Discovered a 3rd `leadAdjustable` exposure (`get_task_catalog_for_category`, reads TASK_CONFIG directly) beyond the WO's named two — fix correctly cascaded there too (reviewer-confirmed genuine fix, not output-hiding: cook_view/buy_list were never in `TASK_KEY_CHOICES`, so the catalog was advertising an override the backend would always reject). Byte-equivalent output confirmed via pre/post snapshot test. `profile_complete` orphan counts both zero, no migration. Full jg suite 826/826. Full history: jg's own `WORK_ORDERS.md` |
| NOTIF-8 | Relocated todo engine | Land relocated+generalized engine (todo channel) on generic `notifiable`+type-key; reconcile 3 kind-vocabularies into one taxonomy; absorb `TaskReminderSent` into `NotificationDelivery` | 2026-07-18 | done | ce7071b | P2a. New `notifications/todo/` subpackage: `engine.py` (domain-free duration/due-expression parsing, visibility/severity rules, lifted+generalized from jg-ferien's `task_engine.py`), `registry.py` (`TodoSeed`/`TodoTypeConfig`/`register_todo_provider`), `models.py` (additive `TodoOverride`, migration `0005`), `service.py` (`sync_todos_for_user`, self-healing materialization onto canonical `Notification`/`NotificationRecipient`), `digests.py` + `send_todo_digests` command (generalizes jg's digest scanner, absorbs `TaskReminderSent` into `NotificationDelivery.digest_threshold`). `NotificationType.mode` gains `"provider"` as a second valid value (validated). Codex-built, independent `reviewer` (round 1: R1/R2 P1 + R3/R4 P2 + R5/R6 P3, all fixed — R1: materialized `due`/`severity` were never persisted into `Notification.content`, so `feed/`/dispatchers would see a todo with neither, forever, since `get_or_create_by_dedup` only applies `content` on first creation, fixed by keeping `content` fresh on every sync; R2: a digest threshold was marked "sent" even when the recipient wasn't opted into email, permanently losing that reminder even after a later opt-in, fixed via an explicit opt-in gate before recording; R3: no per-user exception isolation in the digest scan; R4: record-after-send ordering allowed duplicate emails under concurrent runs, fixed via claim-before-send using the existing partial-unique-constraint retry pattern. Round 2 re-review: all 6 confirmed fixed, one new P3 found (digest delivery rows recorded "sent" optimistically before the send attempt, unlike `notify()`'s pending→resolved pattern) — fixed to record `pending`→`sent`/`failed`, matching precedent). 84 passing. Migration additive-only (confirmed via `makemigrations --check --dry-run --skip-checks`). Version unbumped (2.28.0) — accumulates toward the P2 dcm publish alongside NOTIF-9/10. Closes P2a; unblocks NOTIF-9 (jg-ferien repo) |
| NOTIF-8b | Fix todo self-heal gap | Close self-heal gap NOTIF-8 shipped without: `feed/`/`feed/unread-count/` read raw stored `category="todo"` rows directly with no live-derive, so a todo the provider stopped emitting stayed visible forever | 2026-07-27 | done | f9c1d0d | Fixes NOTIF-8 (ce7071b), still dormant (no consumer). A (primary): `todo/service.py` split into `derive_todos_for_user` (full emitted set, incl. dismissed/done — used by the digest reconcile) and `derive_active_todos` (excludes dismissed/done — used for unread-count/digest-send); `views.py`'s `CanonicalInboxView`/`CanonicalUnreadCountView` now exclude `category="todo"` from the raw query and merge in the live derive, so self-heal is immediate (read-time), not scan-time. B (backup): `digests.py`'s `send_todo_digests` gained `_reconcile_todo_overlays` — deletes a user's `category="todo"` overlay rows absent from the current emitted set (cascades to the parent `Notification` once its last recipient is gone), scoped per-user, never touching a still-emitted or legitimately-dismissed-still-emitted row. Independent `reviewer` (mandatory per WO): round 1 found R1 (P1, live regression — merging todos into `feed/` forced full materialization of a user's entire event-authored notification history on every request via `[*queryset]` unpacking, defeating SQL `LIMIT`/`OFFSET` pagination on an endpoint cockpit already consumes in production/staging, even though cockpit registers zero todo providers) + R2 (P2 — a dismissed/done todo could never appear under any filter incl. the default/`status=done` views, unlike an equivalent event-authored row) + R3 (P3, pre-existing — the unread badge's event-authored count already ignored `done_at`, unrelated to this fix, left as an accepted out-of-scope follow-up per the WO's own do-not-touch-event-behavior clause). R1 fixed via an `if not iter_registered_todo_types(): return event_recipients` short-circuit (the todo-merge Python list only materializes once some app actually registers a provider — currently zero), pinned by a `django_assert_num_queries(2)` regression test proving flat query count regardless of notification volume. R2 fixed by deriving the full emitted set and applying the same per-status-filter semantics to both halves. Round 2 re-review: clean, no findings, both fixes confirmed correct and non-vacuously tested. 91 passing. No migration (schema-neutral, confirmed via `makemigrations --check --dry-run --skip-checks`). Version unbumped (2.28.0). Residual accepted trade-off (documented, not new): once a real todo provider is registered, the feed merge still materializes the full event queryset for that request — acceptable per the "todos per user are few" assumption, worth revisiting if that assumption breaks |
| NOTIF-8c | `TodoOverride.created_by` audit field | Add `created_by` FK (SET_NULL, nullable, `related_name="+"`) to `TodoOverride`, migration `0006_todooverride_created_by`; additive/backward-compatible for all consumers `[approval schema]` | 2026-07-29 | done | 83ee3c3 | P2c-1 follow-up. Found by NOTIF-10's independent `reviewer`+`sec_reviewer` (see NOTIF-10 row): the jg cutover onto canonical `TodoOverride` silently dropped the legacy `EventTaskOverride.created_by` audit trail (never API-exposed, so no external behavior change, but a real internal accountability loss the original NOTIF-10 WO never called out — `[approval schema]`, since NOTIF-10's WO explicitly listed "no schema change here"). Operator decision via `AskUserQuestion` (fix now, not accept/defer) — explicit pre-change approval, not a silent scope expansion. dcm full suite 391 passing (independently re-run). Scoped follow-up `reviewer` on just this delta: clean — verified last-writer-wins `defaults` semantics match pre-cutover legacy code exactly (checked via `git show` on the pre-NOTIF-9 legacy views.py), verified additive/non-breaking for this shared library's other consumers (hram, spesix, survey_app, etc.). v2.31.0 → 2.32.0 (publish-from-main, verified live on PyPI before jg's pin bump). jg-ferien re-pinned to 2.32.0 in the same NOTIF-10 landing commit (e8d5d76) — `upsert_event_todo_override`/`EventTaskOverrideViewSet.create`/the NOTIF-9 data-migration helpers all thread `created_by` through (migration helpers preserve historical attribution from the legacy rows, not the migration runner); jg's `test_manager_can_upsert_override` assertion restored. **ID history: filed as NOTIF-13 in `d0f6843`, renumbered to NOTIF-8c on 2026-07-29** — NOTIF-13 was already allocated to the Layer-1 transport extraction in `docs/design/notifications-messaging-roadmap.md` §4 (committed `a45da43`), whose rows 13-17 had never been mirrored into this register, so the "highest used ID" check could not see them. `8c` chosen because this extends the NOTIF-8/8b `TodoOverride` line. Root cause closed by registering NOTIF-13..17 below. |
| NOTIF-9 | jg adopt + data-migrate | jg registers providers as plugins; data-migrate overlays (ref_id 4-type reparse + duty prefix-less outlier, documented loss-tolerance; clean FK moves for override/sent) while old path runs | 2026-07-18 | done | bb0580b | P2b. jg repo. Depends NOTIF-8 + NOTIF-P2-pre. (was NOTIF-8). Landed with named independent reviewer (caught a real P1: dead `eventId` lookup + over-broad migration fallback, orchestrator-fixed) + 2nd reviewer clean; 865/865. |
| NOTIF-10 | jg read/write cutover | Flip all live reads+writes off the 3 legacy overlay tables onto canonical (derive/digest reads + 4 live endpoints; retire dual-write shims); tables stay | 2026-07-29 | done | jg: e8d5d76, b47ff65 | P2c-1. jg repo (own register: jg `WORK_ORDERS.md`, same row). Depends NOTIF-9. RE-SCOPED from "remove old engine" (that scope was unsafe — tables still live-queried); see notifications-messaging-roadmap.md §4. Codex-built. Named independent `reviewer` + `sec_reviewer`, both clean: no IDOR, no residual legacy-table access (grep-verified), synthetic-dismissal fallback for non-object-backed kinds (e.g. `registration`) verified correct/non-lossy, digest-threshold and self-/event-scoping semantics verified equivalent to legacy. One real finding from both reviewers (R2/S1): canonical `TodoOverride` had no `created_by` equivalent to the legacy `EventTaskOverride.created_by`, silently dropping override-authorship audit data (never API-exposed, so external behavior stayed byte-identical, but a real internal accountability regression) — operator-approved fix, see NOTIF-13. Full jg backend suite 865/865 (re-run twice). |
| NOTIF-11 | jg drop legacy tables | Drop the 3 legacy overlay tables (guarded, one-way) after NOTIF-10 cutover verified | 2026-07-29 | done | bec2a0e | P2c-2. jg repo. Precondition re-verified on 2026-07-30 and held: outside `models.py`, migrations and tests, the only runtime legacy-table access left was `events/todo_migration.py`; the name-matching `EventTaskOverrideSerializer`/`EventTaskOverrideViewSet` stayed because they are live canonical endpoints. Fixed the fresh-`migrate` blocker by neutering jg `events/migrations/0062_notif9_todo_channel.py` to `RunPython.noop`, then added guarded one-way `0063_notif11_drop_legacy_overlay_tables` using `apps.get_model()` and `DeleteModel` x3. Removed the 3 model classes plus `events/todo_migration.py`; trimmed only the legacy-row test scaffolding and added direct tests for the 0063 guard. Operator decision preserved jg `build_tasks_for_user`/`_tasks_for` for coverage and deferred that cleanup to NOTIF-22. Review/PR consequence: any DB that had not yet applied `0062` loses those legacy rows permanently once `0063` runs; moot for prod/staging. Landing SHA + named-review note filled by the orchestrator once green and landed. Independent `reviewer` (Sonnet, author=Codex != reviewer): 2 P2-Findings, beide vor dem Commit behoben. R1: die Guard-Tests deckten nur den `EventTaskOverride`-Raise-Zweig ab - `ReminderDismissal`/`TaskReminderSent` haetten entfernt, invertiert oder auf das falsche kanonische Modell gezeigt werden koennen, ohne dass ein Test anschlaegt; jetzt ueber die drei Paare parametrisiert, pro Fall nur EIN Paar im unsicheren Zustand, damit jeder Zweig einzeln gepinnt ist (der Guard ist die einzige Absicherung eines einweg-`DeleteModel`). R2: beide Register standen bereits auf `done` mit leerem Commit-Feld vor Review und Commit - auf `in-review` zurueckgesetzt und erst hier auf `done` gefuehrt. Orchestrator-Vorabpruefung: Precondition-Beweis neu gefahren (einziger Runtime-Zugriff war `events/todo_migration.py`, keine eingehenden FKs). Die WO nannte zwei Namensfallen, der Sweep fand VIER: zusaetzlich `_build_task_context` (LIVE via `todo_channel.py:32,304`) und die kanonischen Konstanten `EVENT_TASK_OVERRIDE_TASK_KEY_*`/`LEAD_ADJUSTABLE_TASK_KEYS` in `todo_canonical.py` - beide waeren bei einem Namens-Sweep geloescht worden. Alle vier stehen unveraendert. `0062`-Blocker per Variante (a) gefixt (Live-Import raus, `RunPython.noop`); `0063` nutzt durchgehend `apps.get_model()` fuer beide App-Labels und macht damit NICHT denselben Fehler wie `0062`; Guard laeuft vor den `DeleteModel`-Ops, einweg. `build_tasks_for_user` bleibt bewusst erhalten (Operator-Entscheidung, siehe Map B.2) - es traegt 20 von 31 Tests in `test_task_engine.py` und beruehrt kein Legacy-Modell; die Ablose ist NOTIF-22. Ein echter Testfehler von uns: Codex entfernte beim Trimmen den `task_category`-Fixture-Import aus `test_todo_channel.py` (sieht ungenutzt aus, ist aber Fixture-Abhaengigkeit von `task_event`) - 8 Errors, behoben plus Kommentar gegen Wiederholung. Zwei weitere rote Laeufe waren UNGUELTIG, nicht rot: Test-DB-Kollision bzw. Neustart von `jg_local_db` mitten im Lauf durch die parallel laufende cook-Suite einer zweiten Session. Gate final gruen: `pytest events/ tests/test_smoke.py` -> 284 passed. Der Lauf baut die Test-DB frisch, `0063` lief also real durch und der Guard liess leere Legacy-Tabellen korrekt passieren. **Datenverlust-Konsequenz:** jede DB, die `0062` nie angewendet hat, verliert diese Legacy-Daten endgueltig, da die Tabellen im selben WO fallen - fuer prod und staging gegenstandslos (0062 laengst angewendet). Promotion-Ordnung bleibt: NOTIF-10 vor NOTIF-11 nach main. |
| NOTIF-12 | Popup channel | Hook ucm wizard renderer as popup channel; seen-status on `NotificationRecipient`, not onboarding-progress store | 2026-07-18 | done | dcm: 1612429, 82fd89f; ucm: ui-core-micha c8e222f, d7bdcbe | ucm repo (+ dcm scope D: `PopupDispatcher`). (was NOTIF-11; renumbered 2026-07-29 for the jg cutover/drop split). **WO authored 2026-07-29: `ui-core-micha/work-orders/NOTIF-12.md`.** Re-tiered to **Tier 2**: the roadmap's "P3/uncritical" is about business urgency, but the WO extracts the dialog shell out of `OnboardingWizard`, which cockpit and jg run in production — real blast radius. **HARD GATE: starts only after NOTIF-13 landed + published** (consumes its envelope contract + `subscribe()`, and edits the same ucm files). **Ships with ZERO producers, deliberately:** verified 2026-07-29 that no notification type declares `popup` in `eligible_channels` in dcm, jg, cockpit, hram, spesix or survey_app, and `PopupDispatcher` (`dispatch.py:117`) is a logging stub — operator decided to build the scaffolding anyway with that evidence in hand. WO therefore mandates non-overclaiming CHANGELOG wording (NOTIF-4 R1 precedent) plus a test-local producer type proving the path end-to-end. Note for whoever picks this up: the popup use-cases originally cited (PWA install, cookie consent, name completion) turned out to be **onboarding steps**, already served by ucm's `OnboardingProvider`/`CookieConsentStep`/`CompleteNameStep` — they are not popup-channel producers, and the remaining gap there (jg's `WelcomePage` still holding its own cookie/name prompts) is an `ONB-*` topic, not this WO. **Gate satisfied 2026-07-30** (ucm 2.13.0 / dcm 2.33.0). WO re-verified against the landed NOTIF-13 code and corrected (ucm `345631d`): NOTIF-13 shipped `envelope` as a **domain**-level discriminator, so a popup payload built like a chip payload is indistinguishable from it — the WO now mandates a `channel` field inside the envelope (explicitly NOT a second envelope value, which would undo the Layer-1 design) plus feed de-duplication by `notification_id`, without which a chip+popup notification double-counts the unread badge. **Implemented 2026-07-30 directly in Claude** (Codex CLI failed twice with the same `gpt-5.6-terra requires a newer version of Codex` error already seen on NOTIF-13 — genuine, not transient). **Independent `reviewer`: 3 findings, all fixed** — R1 (P1, real bug: WS payload lacked `recipient_id`, so `feed/mark/`, which resolves against `NotificationRecipient.pk`, would silently no-op or mismark an unrelated recipient row for any popup or chip WS push acted on before a feed refresh; fixed by adding `recipient_id` to both dispatchers, regression-tested), R2 (P2: reused dialog shell always showed the onboarding header text even for a popup; fixed with an optional shell title override, onboarding default unchanged), R3 (P2: coexistence only checked *blocking* onboarding steps, so a non-blocking step and a popup could render two dialogs at once; fixed by gating on any active onboarding step). Re-verified green after fixes: dcm 95/95 pytest (notifications package), ucm 88/88 vitest + build clean. Published (once pushed): dcm 2.34.0 (PyPI), ucm 2.14.0 (npm) — both CI-gated on push to `main`. No app pin bumps in this WO. |
| NOTIF-13 | Layer-1 transport extraction | Generalize ucm `NotificationsProvider` into a pluggable subscribe-by-envelope realtime primitive; preserve the S112 consumer contract and the single-socket-owner invariant. (Originally scoped as "+ a dcm multi-stream consumer carrying more than one stream type" — **that turned out to be unnecessary and was NOT built**: dcm's transport was already stream-agnostic, so its share reduced to the `notification_envelope()` contract helper. See the Notiz.) | 2026-07-29 | done | ucm: 7a83ee9, 78cd7bc; dcm: de77335 | Phase A. ucm+dcm. Depends NOTIF-6. **The hinge**: finishes the notifications frontend (unblocks NOTIF-15) and is the substrate Phase B messaging rides on. Precedes NOTIF-15. Envelope: `docs/design/notifications-messaging-roadmap.md` §4; open risk §5 (retiring jg's local `NotificationsContext` must not drop messaging's live updates — the primitive must carry both stream types before jg cuts over). WO authored 2026-07-29: `ui-core-micha/work-orders/NOTIF-13.md` (own register row there, same content). Operator decision: additive only — `NotificationsProvider` stays the sole mount point, no standalone `RealtimeProvider` for apps to mount. Scoping correction found while authoring: dcm needed NO "multi-stream consumer" — `delivery.py::push_to_users` already fanned arbitrary payloads and `consumers.py::NotificationConsumer.message()` is stream-agnostic; dcm's scope E was instead a thin `notification_envelope()` authoring helper (`"envelope": "notification"`, additive) wrapping the two existing WS producers (`views.py`'s status broadcast, `dispatch.py`'s `ChipDispatcher`). Codex failed twice with a genuine CLI/model-version error (`gpt-5.6-terra requires a newer version of Codex`) — orchestrator fell back to direct Claude implementation. Independent `reviewer` (sec_reviewer not required per WO, Tier 2): found R1 (P2, real bug — decoupling the REST seed from the socket connect opened a race where a live push arriving before the initial feed loaded would be silently discarded by `refresh()`'s unconditional `replaceNotifications()`; fixed via a `seeded` state gate restoring the exact original seed-then-connect ordering), R2 (P3 — ucm has no CHANGELOG.md convention; documented via commit + register only per operator decision), R3 (register housekeeping, closed here). ucm full suite 70/70 + `pnpm build` clean; dcm notifications suite 92/92, full dcm suite 392/392. Published: ucm 2.13.0 (npm), dcm 2.33.0 (PyPI) — no app pin bumps in this WO per spec. |
| NOTIF-14 | jg message-notify → `notify()` | Move jg's second notification producer (`messaging/services.py`) off the legacy `deliver_push_email`/`push_to_users` path onto the canonical router + a registered notification type | 2026-07-29 | done | jg: 37ea0a7 | Phase A. jg repo. No dependency (backend delivery only) — can land any time, independent of the transport work. The last jg producer not on the canonical router. Envelope: roadmap §4. **WO authored 2026-07-30: `jg-ferien/work-orders/NOTIF-14.md`.** **Roadmap correction — NOTIF-14 is NOT independent, NOTIF-15 depends on it:** jg sends six messaging WS payloads (`message`, `message_edited`, `message_deleted`, `poll_updated` x2, `reaction`) through `push_to_users`. They are chat live-sync, not notifications, so they must not go through `notify()` — but they are envelope-less, and ucm's Layer-1 router defaults those to the notification envelope (`realtime.jsx:68`), so once NOTIF-15 retires jg's local socket owner all six would land in the feed as fake notifications. The `envelope: "messaging"` tagging therefore belongs in NOTIF-14. Only ONE genuine notification producer exists in messaging: `deliver_push_email` at `services.py:644`. **Operator decision 2026-07-30:** bell = system notifications, chat badge = human messages — the new message type gets `email`+`push`, explicitly NOT `chip`. No dcm bump needed (jg pins 2.32.0; `notify()` shipped in 2.27.0). **Landed 2026-07-30 (jg `37ea0a7`).** Named independent `reviewer` (author=Codex != reviewer), 2 passes on materially different diffs; full review record in jg-ferien `WORK_ORDERS.md`. Two envelope errors of mine surfaced during implementation and were fixed at the platform level: (a) the design would have persisted cleartext excerpts of Fernet-encrypted message bodies into the plain `Notification.content` JSONField, bypassing the audited privileged-read path -> fixed via dcm 2.35.0 `transient=` (NOTIF-19); (b) omitting the `chip` channel did NOT keep messages out of the bell, because the canonical feed filters only `category="todo"` -> fixed via `feed_visible=False`. Also caught pre-commit: Codex's hand-rolled `.mo` writer would have shipped ~82 wrong de/fr strings (recompiled with real `msgfmt`, 182->187 entries). Staging green. |
| NOTIF-15 | jg bell/feed adoption | jg frontend onto ucm `NotificationsProvider`/`NotificationBell` reading `feed/*`; retire jg's local `NotificationsContext` (messaging re-subscribes via the Layer-1 primitive) | 2026-07-29 | done | jg: 5148677 | Phase A. jg repo. **Depends NOTIF-13** (needs the pluggable transport before jg's local multiplexing context can be retired). Envelope: roadmap §4. **WO authored 2026-07-30: `jg-ferien/work-orders/NOTIF-15.md`.** Gate widened: depends on **NOTIF-13 (landed) AND NOTIF-14** (the messaging envelope). Includes the ucm pin bump 2.12.0 → 2.14.0 — that gap carries NOTIF-12's dialog-shell extraction out of `OnboardingWizard`, which jg mounts, so onboarding is a regression surface in this WO. jg has **no bell today** (only a chat-unread badge from `useMessaging()`), so the bell is an additive new surface, not a replacement; the chat badge stays. Verified while scoping: no urlconf work needed (`feed/*` already exposed via `django_core_micha.api_urls:73`), and ucm's `seeded` gate sets in `.finally()`, so a failing feed request cannot take jg's chat offline. **Landed 2026-07-30 (jg `5148677`).** Named independent `reviewer`; full record in jg-ferien `WORK_ORDERS.md`. One envelope error of mine surfaced: the WO named `MessagingContext` as the "only consumer" of the local socket owner — `Thread.jsx`, the actual chat UI, subscribed directly too (five live paths, ~12 dependent test sites). Both consumers were re-subscribed via `subscribe(MESSAGING_ENVELOPE, …)`. jg's local `NotificationsContext` deleted, ucm 2.14.0 adopted, bell added alongside the unchanged chat badge. Staging green. **Residual CLOSED 2026-07-30:** the end-to-end chat round-trip was verified live on staging (prod-synced data) by a cross-repo review driving the deployed app in a browser. Observed: exactly ONE socket `wss://…/ws/notifications/` on the wire; outbound AND inbound messages both arriving as `{"envelope": "messaging", "type": "message"}` and rendering live with no reload; and — as the *recipient*, so with `notify()` genuinely firing — bell `0`, `feed/unread-count/` `{"count": 0}`, `feed/` empty, which is the direct proof of `feed_visible=False` from the receiving side. The deployed bundle carries the Layer-1 router verbatim and no longer contains `registerMessageCallback`. |
| NOTIF-16 | hram `notify()` adoption | hram adopts the canonical `notify()` router (state-only "job done" notifications) | 2026-07-29 | planned | — | Phase A. hram repo. Depends: Layer 2 stable. Parallelizable with NOTIF-17. Envelope: roadmap §4. WO file not yet authored. |
| NOTIF-17 | spesix `notify()` adoption | spesix adopts the canonical `notify()` router (state-only "job done" notifications) | 2026-07-29 | planned | — | Phase A. spesix repo. Depends: Layer 2 stable. Parallelizable with NOTIF-16. Also the first live messaging consumer later in Phase B (MSG-4). Envelope: roadmap §4. WO file not yet authored. |
| NOTIF-18 | Task-digest producer onto `notify()` | jg's `events/task_reminder_digests.py:301` still calls the legacy `deliver_push_email`, live via the `send_task_reminder_digests` management command | 2026-07-30 | done | jg: cbb14e3, eb4c7b4 | Phase A loose end, found while scoping NOTIF-14 on 2026-07-30. jg repo. A **third** legacy producer that neither NOTIF-14 (messaging track) nor NOTIF-11 (table drop) covers — the roadmap does not list it at all. **Open question before authoring:** whether this command is still the live digest path after NOTIF-8/9/10 moved digests to dcm's `send_todo_digests`; check cron/deployment config first — if dead, this is a removal, not a migration. Phase A is not honestly closed while this producer is on the legacy path. WO file not yet authored. **Landed 2026-07-30 (jg `cbb14e3`).** Named independent `reviewer`: no findings; gate green (279 passed). Scope A stop-condition re-verified and held — scheduling is data-driven per app `project.yaml`, jg lists only `send_todo_digests`, and the two `webapp-management` hits are a historical header comment plus synthetic fixture YAML that never read jg's real config. **One envelope miss of mine:** the relocation was bigger than the one function I named — `_candidate_users` calls `_candidate_user_ids`, which also lived in the deleted module, so moving only the former would have raised `NameError` at call time **inside the digest**, not at import. Both were moved and verified byte-for-byte via `diff`. **Second miss:** the WO said "keep or port whatever test coverage exists"; there was none — nothing referenced either helper by name — so the test was written from scratch through the real chain (`register_todo_providers()` → dcm `_candidate_users` → `iter_candidate_users_fns()` → jg's function), covering all four union sources. Reported and deliberately not folded in: the `todo_migration.py:115` comment (that file belongs to NOTIF-11, explicitly do-not-touch) and the cosmetic `webapp-management` mentions. Closes the last legacy `deliver_push_email` producer in jg. |
| NOTIF-19 | Transient dispatch params + feed-visibility policy | [`work-orders/NOTIF-19.md`](work-orders/NOTIF-19.md) — `notify(transient=...)` values render into email/push but are never persisted into `Notification.content` nor enter the `dedup_key` (wired through the previously-dead `ctx` on `Dispatcher.deliver`); `NotificationType.feed_visible=False` excludes a type from `feed/` and `feed/unread-count/`. Version 2.34.0 → 2.35.0 | 2026-07-30 | done | bea6ad0 | Independent `reviewer` (Sonnet, author=Codex ≠ reviewer): no blockers; 3 minor findings, all resolved before push — R1 the review artifact omitted `CHANGELOG.md` (reviewer read it separately and confirmed it accurate), R2 this register row was missing (added), R3 `feed_visible` had no signal that it does not apply to provider-derived todo rows (comment added at `types.py`). R4 recorded as residual, not a defect: the new render-fallback warning's log volume across the other 13 consumers is unverifiable from this repo — it logs, it does not change behaviour, and no other app is on 2.35.0 yet. Published to PyPI 2026-07-30 (run 30543551498, success). Raised by two findings during the jg NOTIF-14 review, not from the roadmap: (1) `notify()` had no way to give a dispatcher a value without persisting it, so jg's Fernet-encrypted `Message.body`/`title` would have gained a cleartext, unaudited second copy in an unencrypted `JSONField`; (2) omitting the `chip` channel suppressed only the realtime push — `CanonicalInboxView`/`CanonicalUnreadCountView` excluded solely `category="todo"`, so a delivery-only type was still fully visible in the bell. Both are the same root cause: delivery-only was not expressible. Strictly additive; both features default to today's behaviour. Empty-`__in` exclude verified to emit no `WHERE` clause (checked empirically **and** against Django 6.0.6 `sql/where.py`), so the 13 repos on older pins see no query change. Full dcm suite 400 passed. **This WO's push is the 2.35.0 PyPI publish** (operator-authorised); consumers pick it up only on pin-bump — jg NOTIF-14 is the first. |
| SEC-1 | `sync-secrets` destination matrix + retire `.env.local` | Destination matrix for `sync-secrets`: no flags → local `.env` + all `bare_server_targets` GH envs (today staging+prod, gracefully skipping the local half when `project.yaml` has no `local` env — e.g. `webapp-management`); `--local` → local `.env` only (fatal if no `local` env); new `--github`/`--remote` → all GH envs only, no local write; `--staging`/`--production`/`--server` unchanged. Local write now delegates to `generate_env.generate_env(env_name="local", ...)` (lazy-imported to avoid the circular import with `generate_env.py`'s module-level `sync_secrets` import) instead of the old secrets-only `sync_local()`/`.env.local`, both deleted. Bare-mode GH loop extracted unchanged into `_sync_all_github_targets`, shared by no-flags and `--github`/`--remote`. Companion fix in `generate_env.py`: `load_inputs_from_secrets_yaml` now passes `project_config` into `resolve_source`, needed for `{server}`-template resolution through this path (previously only the GH path had it) | 2026-07-28 | done | (uncommitted at register-time; see commit note) | v2.29.0 → 2.30.0 (publish-from-main). Codex-built (background run stalled without a clean `RESULT:` line after making the core edits; orchestrator picked up the uncommitted diff per the Codex→Claude fallback rule, fixed one test bug — a `{server}`-template test assumed an implicit `secret_target` default that was never in spec, fixed by passing `--secret-target local` explicitly — and added one missing test (`--github`/`--remote` + `--secret-target` rejection, P3 from review). Independent `reviewer`: one P3 (test-coverage gap only, code already correct) — fixed. Full dcm suite 374 passing. Consumers re-pin dcm when ready (separate, gated, per app) |
| SEC-2 | Cloudflare Turnstile for open self-signup (dcm+ucm+jg pilot) | Bot-check on `self_signup_open`/`self_signup_email_domain` only (access_code/qr/admin_invite untouched). Gated entirely on `TURNSTILE_SECRET_KEY` presence — unset ⇒ pure no-op, no consumer breaks on bump; no AuthPolicy migration. dcm: `TURNSTILE_SITE_KEY`/`SECRET_KEY` via `env(default="")`; new `auth/turnstile.py` (stdlib `urllib` siteverify, fail-closed, hostname-checked against `ALLOWED_HOSTS`); `turnstile_token` field on `RegistrationRequestSerializer`; one guard in `register_request` right after `is_valid()`; `turnstile_site_key` in `build_public_auth_config()` only when secret set. ucm: `TurnstileWidget.jsx` (idempotent CF script inject, explicit render, token callback, reset) + `SignUpPage.jsx` renders it only when site-key present and mode is open/email_domain, submit gated on token, token threaded through `submitRegistrationRequest`. jg-ferien pilot: `secrets.yaml` shared-source line (`proton://webapp-management/turnstile/secret_key`, CF test dev_default) + `project.yaml app_env`/`env_overrides` site key — config only, no re-pin | 2026-07-28 | done | dcm: (this commit); ucm: separate ui-core-micha commit; jg-ferien: config staged, uncommitted (see Notiz) | v2.30.0 → 2.31.0 (dcm, publish-from-main) / ucm 2.11.0 → 2.12.0 (separate commit). Codex-built per repo (dcm run hit a nested self-delegation timeout mid-run and correctly fell back to implementing directly, per its own progress narration — no orchestrator intervention needed). Independent `reviewer` (correctness, no findings) + `sec_reviewer` (security, no findings — noted one non-exploitable library-level observation: `verify_turnstile_token`'s own hostname check no-ops on an empty `allowed_hostnames` rather than failing closed, but the only current caller pre-guards against that, so not currently reachable) — both clean, both ran on the full 3-repo diff. dcm full suite 391 passing (independently re-run); ucm vitest 65 passing (independently re-run). **jg-ferien's `secrets.yaml`/`project.yaml` config edits are reviewed-clean but deliberately left UNCOMMITTED** — `TURNSTILE_SITE_KEY` in `project.yaml app_env` is still an operator placeholder; re-pinning dcm/ucm in jg-ferien and committing its config are the remaining gated finalize steps, blocked on the operator provisioning the real shared Cloudflare Turnstile widget (all app hostnames) and storing the real secret at `proton://webapp-management/turnstile/secret_key` |
| NOTIF-20 | Turn on notification retention | Schedule the never-scheduled `prune_notifications` janitor where canonical notifications are live (jg-ferien now; cockpit if the mechanism supports it), with a mandatory dry-run gate before the first real deletion. Full spec: [work-orders/NOTIF-20.md](work-orders/NOTIF-20.md). | 2026-07-30 | dropped | — | Tier 2 (recurring bulk DELETE on production data + deployment config). **Live problem, not hygiene:** verified 2026-07-30 that NO repo schedules `prune_notifications` — not jg, cockpit, hram, spesix or survey_app — so the janitor shipped in NOTIF-4 (2.27.0) has never run anywhere. jg-ferien now writes one `Notification` + one `NotificationRecipient` per chat message (NOTIF-14, `37ea0a7`), invisible by design via `feed_visible=False` (NOTIF-19), so nothing surfaces the growth; cockpit has accumulated unpruned since NOTIF-7. Raised by the NOTIF-15 Orchestrator session, which correctly flagged it instead of expanding scope. **BLOCKED 2026-07-30 (operator: pause + re-author).** Orchestrator Scope-A/B investigation found the envelope's premise does not hold for production: `scheduled-commands.yml` resolves targets by the `scheduled_commands` role, and `webapp-management/project.yaml:82` grants that role to **`staging` only** (`main-prod`/`contact-prod`/`innoservice-prod` carry `[traefik, backup, maintenance, janitor, ssh_sync, restore]`). So adding `prune_notifications` to `infra.scheduled_commands` would prune staging only, while production jg - which has written one `Notification` + one `NotificationRecipient` per chat message since NOTIF-14 (`37ea0a7`), invisible via `feed_visible=False` - keeps growing. That is the WO's own 'registered but never fires' risk, and its Expected Outcome would read as done while being false in prod. Granting the prod boxes the role is platform-repo, prod-affecting deploy config, which this WO explicitly forbids building. Scope B answered en route: the resolver is generic (`resolve_app_inventory.py:188-205`, missing `infra:` yields `[]`), so a `project.yaml` block IS mechanically sufficient for cockpit - same staging-only ceiling. Second-order finding, outside this WO: `send_todo_digests` uses the same role-gated mechanism, so jg's live todo digest also appears to run on staging only. Nothing implemented, nothing committed; scope C dry-run not exercised. Full finding in [work-orders/NOTIF-20.md](work-orders/NOTIF-20.md). **DROPPED 2026-07-30 by operator decision.** Retention brings no useful benefit at these volumes: with a 90-day cutoff nothing would be deleted for 90 days anyway, and the privacy concern that originally motivated it is already solved by NOTIF-19 `transient=` (chat text is never persisted into `content`). The janitor stays in dcm, unused and reactivatable if a consumer ever produces high volume. **The WO was still worth its cost:** the Orchestrator investigation surfaced that the `scheduled_commands` role is granted to `staging` only, so no app command has ever run in production — which turned out to be CI-3's own documented staging-first gate awaiting its completion step, now authored as `webapp-management/work-orders/CI-5.md`. |
| NOTIF-21 | Per-type notification retention | Expose `expires_at` (or a per-type default TTL) on the `notify()` authoring API so short-lived notifications — chat messages above all — expire in days instead of riding the global 90-day `NOTIFICATIONS_RETENTION_DAYS`; then set it for jg's message type | 2026-07-30 | dropped | — | dcm + jg. Depends NOTIF-20 (bound the growth first, then tune it). **Verified 2026-07-30:** `notify()` does NOT currently accept `expires_at` — the signature ends at `transient` — even though `Notification.expires_at` exists since NOTIF-1 and `prune_notifications` already honours it. So this is a real dcm API gap, not a jg call-site fix. Deliberately split from NOTIF-20 so an urgent app-side fix does not drag a dcm release, PyPI publish and pin bump along with it (lesson from the NOTIF-14/NOTIF-19 session). WO file not yet authored. **DROPPED 2026-07-30 by operator decision**, together with NOTIF-20 — a per-type TTL only matters once retention runs at all, and it does not. The underlying gap is real and recorded here for whoever needs it later: `notify()` still does not accept `expires_at`, although `Notification.expires_at` exists and `prune_notifications` honours it. |
| NOTIF-22 | Canonical-path task coverage; retire `build_tasks_for_user` | Deepen jg's live-path tests for the five thin task kinds, re-pin the TE-2 duty regression on the canonical path, then delete the dead legacy assembly wrapper | 2026-07-30 | done | jg 1f52c92 | Tier 1, low priority, blocks nothing. jg repo; full spec `jg-ferien/work-orders/NOTIF-22.md`. **Gate: after NOTIF-11**, which deliberately keeps `build_tasks_for_user` (operator decision 2026-07-30) so its table drop stays a clean schema change — the function is dead at runtime but touches none of the 3 legacy models, so it blocks nothing there. Arises because `build_tasks_for_user` is a dead *assembly wrapper around live rules*: `todo_channel.py:23-48` imports the entire derivation set from `task_engine`, so rule changes break both test files and only assembly changes slip through. Measured coverage gap on the live path: 1 assertion each for `buy_list`/`cook_view`/`cook_signup`/`reimbursement_submit`/`registration_incomplete` vs 3-10 on the legacy path. Substantive item is the TE-2 regression pin (`test_task_engine.py:985`: a cancelled/rejected registration keeps a live duty via `GroupMembership`, hence the deliberately unscoped event fetch) — if the canonical path does not reproduce it, that is a real cutover regression and the WO ends BLOCKED. Independent `reviewer`, 2 Paesse (Autor=Codex != Reviewer). Pass 1 fand VIER Abdeckungsverluste, die die erste Runde hinterlassen hatte - `ParticipantCharge`-gesteuerte Zahlungssichtbarkeit (geldnah), `travel_info` (fehlte sogar in der Abdeckungstabelle der WO selbst), Lead-Override-Wirkung, `reimbursement` Claim/Grace - alle per Folgerunde auf dem kanonischen Pfad geschlossen (7 neue Tests). Pass 2 bestaetigte die Folgerunde und fand einen MAJOR: Scope C hatte den Wrapper geloescht, aber `_get_event_task_configs`/`_build_provider_tasks_for_event`/`_open_provider_items`/`PROVIDERS` als geschlossenen toten Cluster (65 Zeilen) im Produktivmodul stehen lassen - entfernt. **Scope B: nicht blockiert** - der kanonische Pfad liefert die Dienst-Aufgabe weiterhin bei `cancelled` Registration mit ueberlebender GroupMembership; keine Regression aus NOTIF-9/10. TE-5 (jg `0287b8e`) wurde bewusst vorher gelandet, damit die `reimbursement_submit`-Tests das korrigierte Verhalten pinnen. Gate: `pytest events/` 280 passed. |
| DX-1 | Survivable parallel test runs: session-unique test DB + the `timeout` trap | Opt-in env-derived `DATABASES["default"]["TEST"]["NAME"]` in `settings/settings_base.py` (byte-identical default when unset), plus an approval-gated doc proposal for the `timeout`-kills-the-client-not-the-container gotcha. Full spec: [work-orders/DX-1.md](work-orders/DX-1.md). | 2026-07-30 | planned | — | Tier 2 — shared base settings, so critical-tier test scope (blast radius across all apps) applies. Low urgency, blocks nothing. From a real incident in the NOTIF-22 session: a killed run kept its container alive for 26 minutes, held the test DB and drove Postgres to 92%, costing two wrong diagnoses (a test wrongly called "pre-existing broken", and the interference wrongly attributed to the parallel session). **Two problems, deliberately kept apart:** the *cause* was `timeout` killing only the `docker compose run` client (documentation fix), the *amplifier* was one shared `test_<NAME>` per app — neither dcm `settings_base.py:149` nor jg `test_settings.py` sets a `TEST` key — which is the code fix. Fixing only the amplifier would leave the actual cause in place. |
| NOTIF-23 | cockpit catch-up to dcm 2.35.0 / ucm 2.14.0 | Bump cockpit's two shared-core pins across 7 dcm and 3 ucm minors, apply the two additive migrations, regression-verify the three ucm components it mounts | 2026-07-30 | in-review | cockpit: 1cdafb2 | Tier 2, `[approval schema]`. cockpit repo; full spec `cockpit/work-orders/NOTIF-23.md`. **Debt, not breakage:** cockpit at 2.28.0/2.11.0 is internally consistent and is the last consumer still on the pre-NOTIF-13 envelope contract; closing it retires the two-contracts state in the estate. API compatibility pre-verified while authoring — `NotificationBell({resolveLink})` unchanged, all `NotificationType` fields added since 2.28.0 are defaulted, and either bump order is safe in both directions. Substance is the inherited behaviour, not the diff: `seeded` socket gate (NOTIF-13 R1), feed dedup by `notification_id` (NOTIF-12 D2), the extracted `OnboardingWizard` shell (main regression surface, unrelated to notifications), and the NOTIF-8b todo-merge short-circuit that preserves `feed/` pagination for a zero-provider consumer. Sharpest risk: verifying Turnstile (SEC-2, dcm 2.31) stays inactive — a bot-check on the monitoring app's login is a lock-out, and cockpit is what watches everything else. **Implemented 2026-07-30 (Codex, cockpit `1cdafb2`):** pins bumped to dcm 2.35.0 / ucm 2.14.0, lockfile regenerated, no dcm/ucm source change. Independent `reviewer` (author=Codex ≠ reviewer): no blocking findings — verified against the actually-installed packages (not just the WO's claims) that `NotificationBell({resolveLink})`'s contract, `NotificationType` field defaults, the NOTIF-8b todo short-circuit, and the Turnstile no-op gate all hold as stated. Orchestrator independently re-ran cockpit's full suite inside its Docker container: 282/282 backend, 18/18 frontend, `vite build` clean. Residual noted by review (not a defect, matches the WO's own acceptance criteria): the onboarding wizard's actual rendering has no dedicated frontend test — remains a live-staging check, not a code gap. **Remaining before `done`:** operator's live staging verification (both migrations apply cleanly, a real status-change notification reaches the bell and clears, onboarding wizard renders/behaves, `feed/` pagination holds, login works with Turnstile inactive, `healthz` 200). |
| NOTIF-25 | Publish CI broken for every commit since NOTIF-24 | A historical-migration test (`test_0003_migration_preserves_legacy_channel_behavior_without_seeding`, `test_preference_matrix.py`) rolls the test DB schema back to migration 0002 then forward to 0003 to test legacy behavior, then restores it via a `finally` block hardcoded to `MIGRATION_0003` — correct when 0003 was the latest migration, but NOTIF-24 added 0005/0006 (`TodoOverride`), and Django's test teardown (`flush`) never re-runs migrations, so this test permanently pinned the schema at 0003 for the rest of any session it ran in, ahead of anything needing 0004+. Surfaced when it broke `test_bulk_upserts_converge_when_rows_appear_in_the_race_window` (`no such table: ...todooverride`) on two consecutive, otherwise-unrelated publish runs. | 2026-08-02 | done | c27e741 | No WO file — surfaced as a CI blocker mid-session while publishing MSG-6b, operator approved fixing it inline. `finally` block changed from the hardcoded historical target to `call_command("migrate", run_syncdb=True, verbosity=0)`, which restores to the actual latest state regardless of what ships after. A first hypothesis (switch `tests/settings.py`'s SQLite `NAME` from `:memory:` to a file, suspecting a connection-loss issue) was tried, did NOT fix it, and was reverted — the real cause was the hardcoded migration target, not the in-memory DB. Independent `reviewer`: no findings, confirmed only this one file changed and the `:memory:` revert held. Verified stable across 3 consecutive full-suite runs (485/485 each), including both orders (isolated and combined with the previously-failing test). |
| NOTIF-24 | Todo derivation issues per-seed writes on every read | `derive_todos_for_user` loops every registered type, then every emitted seed, and per seed issues its own `TodoOverride` filter, a `get_or_create_by_dedup` (SELECT + sometimes INSERT), a conditional `notification.save(update_fields=["content"])`, and a `NotificationRecipient.get_or_create` — roughly 3–4 queries **per todo**, on a GET, several on the write path. Separately, `CanonicalUnreadCountView` computes `todo_count` via `derive_active_todos`, so producing **one integer** for a badge runs every provider and materializes every notification and recipient row. Batch the overrides, the dedup-key lookups, the creations and the stale-content re-syncs to a bounded per-derivation count, and give the count path something cheaper than full materialization. | 2026-08-02 | done | v2.39.2 | Measured 2026-08-02: 13-todo fixture went from 39 queries (cold and warm alike) to 8 cold / 3 warm. `reviewer` (Sonnet, background, full diff): TOCTOU/race safety holds (bulk_create+ignore_conflicts+re-fetch on the `dedup_key`/`(notification,user)` unique constraints, exercised by a genuine race-window test, not just two sequential calls); no new cross-user badge leak (count query is coarser but keyed correctly per candidate pair). One pre-existing, out-of-scope finding surfaced and NOT fixed here: `CanonicalInboxView`'s `status=unseen` filter doesn't exclude dismissed/done rows the way `CanonicalUnreadCountView`'s badge does, for both event and todo rows — badge/list can disagree on a dismissed-but-unseen item. Predates this WO, affects the general inbox view (not just todos), flagged as a follow-up task (chip `task_1ad7fbe7`), not blocking this commit. Own scoped test run: 34 passed (`test_todo_service.py` + `test_canonical_notification_api.py`); fixed one flaky query-count assertion (`==`→`<=`) caused by `ContentType.get_for_models`'s process-wide cache. | Envelope: [work-orders/NOTIF-24.md](work-orders/NOTIF-24.md) — **Part A only; the implementation map is the Orchestrator's to write.** Approval Gate #1 granted by the operator 2026-08-02. Surfaced from a jg-ferien operator report ("dashboard notification/task query takes forever"); jg owns its own share as `PERF-4` (its providers rebuild their whole context once per registered type), these two costs sit here and hit **every** consumer. **The writes are deliberate, not a bug** — the docstring says so: the upserts exist so per-user `seen_at`/`dismissed_at`/`done_at` survives derivation, and `get_or_create_by_dedup` is what makes it idempotent and TOCTOU-safe. A read-only "fix" would resurrect dismissed todos. This WO batches the writes, it does not remove them. **Tier 2** (shared-core, notification write path, concurrency semantics currently guaranteed by `get_or_create` that any bulk rewrite re-implements). Named risks: losing TOCTOU safety (a naive SELECT-then-`bulk_create` is unsafe unless it handles the conflict — mandatory test), silently dropping the stale-content re-sync (a stale row is a wrong due date shown to a user, reads as a data bug), and count/list divergence if the badge gets a cheaper path (explicit equivalence test). Contract frozen: `TodoSeed`/`TodoTypeConfig`/`register_todo_provider`/`candidate_users_fn` keep their shapes — jg's `PERF-4` is being written against today's contract and must not break mid-flight. Independent of `PERF-4`, either order; if both land, jg re-measures since the two multiply rather than add. |
| MSG-1 | Messaging v1: requirements + design doc | Binding design for the shared messaging domain: generic scope model (container / object-anchor / app-global), policy + membership hook interfaces, multi-app encryption-at-rest key management, complete REST+WS contract on the Layer-1 `messaging` envelope (four contract questions per notification-producing event), attachment pipeline (PDF+Office+images, ~25 MB, magic-byte allowlist, download-only, scan-hook seam without infra), notification volume/retention re-assessment (NOTIF-20/21 wiedervorlage), Phase C migration sketch, MSG-2/3/4 chunk+release plan; includes formal spesix demand confirmation as Go/No-Go | 2026-07-31 | done | — | Phase B start. Envelope: [work-orders/MSG-1.md](work-orders/MSG-1.md). Doc-only WO (no code; deliverable `docs/design/messaging-platform.md`). Tier 2; independent review of the document required (author ≠ reviewer); no tests (deliberate — validation = two paper tests in the doc + review + Gate #1). v1 feature scope fixed in operator scoping session 2026-07-31 (jg-parity floor + object-thread kind + 4 schema seams + file attachments; OUT: search, typing (v1.x, REST-ping), WS-inbound, scan infra). **Implemented 2026-07-31 (Codex):** `docs/design/messaging-platform.md` covers all 10 deliverables — generic scope model, hook contract, encryption hard gate (with STOP clause), full REST/realtime/notification contracts, volume/retention, attachment pipeline, ucm surface, Phase C sketch, MSG-2/3/4 chunk plan — plus both paper tests worked through concretely and the Go/No-Go gate left explicitly open. Independent `reviewer` (author=Codex ≠ reviewer): one P2 finding (volume-math discrepancy in the stress-case scenario), fixed by the orchestrator (recomputed to 1,250,000/day, 37.5m/month, consistent with the stated 500×50×50 inputs); no other findings. **Second reviewer pass (operator + orchestrator, 2026-07-31):** cross-checked the doc against jg source directly (models.py, notifications.py, fields.py, services.py, envelope.py) — all factual claims (5 WS types, `notify()` recipe, delivery-failure handling, key-rotation precedent) confirmed accurate; two gaps found and closed in the doc: (1) OOXML/ODF-vs-ZIP magic-byte ambiguity now documented as an explicit MSG-2 pre-check, (2) `MessagingAuditEvent`'s relationship to the existing `django_core_micha.auditlog` registry (a *write*-audit, model-registration facility — does not fit messaging's *read*-access break-glass need) now documented as a deliberate separate model. **Go/No-Go:** "all three spesix forms wanted" was already the confirmed operator scoping-session decision (2026-07-31), correctly captured in the doc as the stated assumption — not a new open item. Approval Gate #1 passed (operator go-ahead to MSG-2, 2026-07-31). **Still open, blocks only MSG-4 (not MSG-2/3):** concrete expense-claim content-type/participant-derivation rule, moderation/broadcast roles, timeline. |
| MSG-2 | dcm messaging domain | Domain models + services + REST/WS on the Layer-1 transport + `notify()` coupling (`transient=`, `feed_visible=False`) + NEW `notify(expires_at=…)` API + 30d-TTL recipe; full jg-parity floor + object-thread kind + delivered/retention/per-conv-prefs/archive seams + file attachments (incl. OOXML/ODF-vs-ZIP pre-check), per MSG-1 design | 2026-07-31 | done | chunk 1: 7df9670, chunk 2: 858f705, chunk 3: a6a4cf5, chunk 4: 3ad7709, publish: 1d8c60d | Envelope authored 2026-07-31: [work-orders/MSG-2.md](work-orders/MSG-2.md). **Consumer-agnostic** (operator re-ordering 2026-07-31: nothing spesix-specific; jg = first intended beneficiary via MSG-5). Tier 2 `[approval schema]` (additive-only). Broad WO: chunks 0–4, **staged per-chunk commits with independent review per chunk** (operator decision; chunk-1 review includes a security pass on the crypto path); small sibling-repo (ucm) contract-fix chunks in scope (NOTIF-19 lesson). One publish at WO end. Codex-first (code WO — unlike doc-only MSG-1). Chunk 0 resolved without a Codex run (`filetype` already handles OOXML/ODF, folded into chunk 4). Chunk 1 (models + fail-closed per-app encryption) and chunk 2 (policy hooks + services + break-glass audit) landed; both had independent-review findings fixed before commit (chunk 1: thin crypto test coverage; chunk 2: DM break-glass privilege escalation, broken Postgres idempotency-race recovery, recipient-resolution timing). **Chunk 3 (REST + realtime) first pass found materially incomplete + two P1s by independent review** (client-supplied `app_key` trust boundary with no server-side tenant binding; soft-deleted content not cleared and re-broadcast live on `message_deleted`; poll/thread/scope-config endpoints unwired despite being chunk-3 scope) — **operator decisions recorded in `docs/design/messaging-platform.md` §"Tenant resolution and deletion semantics"** (app_key never client-trusted, server-side resolution via scope or the single-active-registration registry function, fail-closed on 0/N registrations; soft-delete clears content; message_deleted frame carries no content). **Chunk 3 landed** implementing all four operator decisions: `resolve_messaging_app()` (models.py) resolves tenant server-side (scope→scope.app; no scope→exactly one active `MessagingApp` else fail-closed 400/409), `app_key` is never read from any request; `soft_delete_message` now clears body/title/link_target and `message_deleted` frames carry only message_id/deleted_at/deleted_by; poll/thread/scope-config REST endpoints wired (create/vote/close, thread get/read, config get/patch); viewer-vs-participant checks split per design §REST table per endpoint, DM carve-outs (no recipient detail, break-glass always denied on DIRECT) unchanged. Final re-review on the complete assembled diff: two minor findings fixed before commit (poll endpoint used `dict(request.data)` directly — fragile against form/multipart QueryDict list-values, replaced with a proper `PollInputSerializer`; missing permission tests for config/reactions/archive/preferences/unread-count endpoints — added). One P3 left as an open question for the operator, not blocking: `DirectConversationView`'s scoped-tenant check requires the target user to already be an established participant of some conversation in the resolved app, which forecloses a scoped first-contact DM with a brand-new user — the design's one-line "validated against the resolved tenant" doesn't nail this down either way. 135/135 full dcm suite green. **Chunk 4 (LAST chunk) landed**: attachment pipeline (`attachments.py` — magic-byte allowlist via `filetype`, image re-encode/EXIF-strip/thumbnail, per-app encryption of blob+thumbnail+filename, scan-hook seam, download-only `nosniff` streaming), `notify(expires_at=…)` exposed on the existing (pre-built) TTL-capable `notify()`/`prune_notifications` machinery, and `messaging.notifications` (per-app opt-in notification type, jg-parity email+push/no-chip/`feed_visible=False` recipe, 30-day TTL, sensitive preview only in `transient=`). Independent review found one P1 (attachment rejections raised `django.core.exceptions.ValidationError`, which DRF's stock handler never translates — every rejected upload crashed with an unhandled 500 instead of 400) plus two P2s (zero test coverage for the notify/TTL wiring; hardcoded non-i18n error strings) and one P3 (legacy Office `.doc/.xls/.ppt` silently dropped from the allowlist, undocumented) — all fixed and independently re-verified clean (dedicated 400-vs-500 regression test added, `test_notifications.py` added covering the full recipe + `on_commit` end-to-end delivery + failure isolation from the durable message, `gettext_lazy`-wrapped, legacy-Office omission now documented as a surfaced deviation in the design doc). 146/146 full dcm suite green. **Published 2.36.0** (`pyproject.toml` bump + `CHANGELOG.md` entry, commit 1d8c60d) — no consumer pins bumped in this WO; MSG-3/MSG-5 pick up the release via their own registry live-check before pinning. **Independent review named per WO: `reviewer` (author=Codex ≠ reviewer) ran once per chunk (4 chunks, chunk 3 twice due to the operator-decision rework) — no open findings remain.** |
| MSG-2b | Scoped first-contact DM must be possible | Remove `DirectConversationView`'s participant-existence precondition so a scope-anchored direct conversation can be **started** with a user who has no prior conversation in the tenant; target-side tenant safety rests solely on `MessagingPolicy.can_open_direct`, which `open_direct()` already enforces before any row is created. Tenant resolution, the `app_key` boundary and self-DM rejection stay unchanged. | 2026-07-31 | done | fb6a7d2 (publishes 2.36.1) | Extends the landed MSG-2 (2.36.0), same convention as NOTIF-8b/8c. Envelope: [work-orders/MSG-2b.md](work-orders/MSG-2b.md). Tier 1, no migration. Origin: the P3 left open by MSG-2 chunk 3's final review, raised to a real defect by the Expertenchat — the check both forecloses first contact (a DM could only be continued, never started) and is not a boundary (any user gets a participant row after one conversation), while sitting *in front of* the `can_open_direct` hook and pre-empting it. Blocks jg parity at MSG-5: jg's `NewDirectMessageDialog` opens DMs against arbitrary event members with no prior conversation. Root cause was an ambiguous sentence in the MSG-2 design addendum ("target validated against the resolved tenant"), corrected in `docs/design/messaging-platform.md` §"Tenant resolution and deletion semantics". Operator decision 2026-07-31 (= Approval Gate #1): first contact must be possible. Sequenced **before MSG-3**, so the ucm composer and conversation launchers are not built against behaviour that would change at adoption. **Implemented (Codex, one pass, no rework needed):** the 4-line precondition removed from `DirectConversationView.post`; comment corrected to state tenant resolution (core) vs. addressability (`can_open_direct`, app) explicitly. 4 new tests (first-contact succeeds + hook genuinely consulted with correct kwargs; policy denial → 403 with zero rows created, not just the right status code; self-DM still rejected; global DM fails closed on 0/N active `MessagingApp` registrations) plus the existing tenant-resolution regression test's target user corrected from "viewer" (already had a participant row, would have masked this exact bug) to "outsider" (zero rows). Independent `reviewer` (author=Codex ≠ reviewer): no findings — confirmed no other code path depends on the removed invariant, `resolve_messaging_app`/self-DM rejection/`app_key` boundary byte-identical, release correctly scoped as a patch. 150/150 full dcm suite green. Published 2.36.1 (commit fb6a7d2, same commit as the fix — patch fixes are single-commit, no separate publish step). |
| MSG-2e | Declare the messaging runtime dependencies and republish 2.38.0 | `Pillow` (`messaging/attachments.py:59`, the whole image pipeline) and `cryptography` (`messaging/crypto.py`, Fernet/MultiFernet — the **encryption-at-rest hard gate**) are imported at runtime but declared nowhere in `pyproject.toml`. Add both to `dependencies`, add a guard test that every third-party module imported under `src/` is declared (must catch lazy in-function imports), and republish **2.38.0** — it exists in pyproject and CHANGELOG but never reached the registry. | 2026-08-01 | done | 80550c5 (publishes 2.38.0) | Envelope: [work-orders/MSG-2e.md](work-orders/MSG-2e.md). **Blocks jg MSG-5a**, which stopped at its mandated step-1 registry live-check with zero files changed and surfaced the gap instead of patching dcm from a jg session — the STOP-and-surface rule working. **Root cause:** the MSG-2d publish run failed its own pytest gate before publishing (2 failed / 465 passed, `ModuleNotFoundError: No module named 'PIL'`), so the release silently never happened while the repo said 2.38.0. **This is `DX-2` working, not backfiring:** before DX-2 added `messaging/tests` to `testpaths`, `test_attachments.py` had never run in CI, so the missing dependency was invisible; DX-2's envelope predicted "if it goes red, that is the finding". No consumer was harmed — the failure stopped the publish. **`cryptography` is the quieter half:** it resolves today only transitively via `django-allauth[mfa]`, so the platform's central security guarantee rests on another package's optional extra. **The graceful degradation hid the severity:** `attachments.py` catches the ImportError and raises a ValidationError that reads like policy, so a consumer without Pillow would have silently non-functional image attachments — jg carries Pillow for its own messaging and would not have noticed; a greenfield consumer would have shipped it broken. `[approval]` for the two dependency additions, covered by the operator handover of this WO. Republish as 2.38.0, not 2.38.1 — the version was never taken, and a green workflow is not proof of a publish. **First Codex invocation self-nested** — it read Part B's "Execution directive" (meant for the Orchestrator) as an instruction to shell out to `codex exec` on itself, failed on a PowerShell/Bash quoting mismatch, then began drifting into an unrelated fallback implementation; killed before any file was touched. Fixed by adding the template's mandatory Preamble block (explicitly stating "you are the implementer, this file's execution-directive line describes how you were launched, not an instruction to launch further") — the retry ran cleanly, PLAN → 5 steps → `RESULT: DONE`. **Implemented (Codex, single pass after the retry):** `"cryptography>=42"` and `"Pillow>=10"` added to `pyproject.toml` `dependencies`; existing `[2.38.0]` CHANGELOG entry amended (no new version heading); new `tests/test_declared_dependencies.py` — AST-walks every `.py` file under `src/django_core_micha/` (excluding `tests` dirs), flags any third-party top-level import (stdlib/`django_core_micha` excluded) not present in a hardcoded `MODULE_TO_DISTRIBUTION` map with a declared `pyproject.toml` distribution; Codex demonstrated it red (temporarily removing `Pillow`, confirming it names `PIL` + `attachments.py:59`) before restoring. `attachments.py`'s guarded import and `crypto.py`'s unguarded fail-closed import both left untouched, as required. **Independent `reviewer` (author=Codex ≠ reviewer):** no findings — independently cross-checked every third-party import under `src/django_core_micha/` (excluding tests) against `MODULE_TO_DISTRIBUTION` via its own AST script (exact match, no gap), verified the version-specifier stripping logic against every current dependency string, sanity-checked the `>=10`/`>=42` floors against installed 12.0.0/46.0.3. Full `python -m pytest -q`: **468/468 passed** (467 MSG-2d baseline + 1 new guard test) — full suite run, not narrow, per this WO's own risk section (dependency change, cross-suite blast radius). Published 2.38.0 (commit 80550c5, `publish.yml` run 30693692962 — `Publish to PyPI` step confirmed it actually executed, not just green-but-skipped). **Confirmed resolvable: `pip index versions django-core-micha` → `django-core-micha (2.38.0)`.** `jg-ferien` MSG-5a is unblocked. |
| MSG-3 | ucm messaging surfaces (full parity) | ConversationList/Thread/Composer + reactions/polls/read-ticks/attachments as shared ucm components; cursor pagination + infinite scroll + optimistic send from day one; i18n de/en/fr; PWA/reconnect = Layer-1 semantics | 2026-07-31 | done | ucm: `affa5ac`, `5e87315`, `a12c9c6`, `c7100b1`, `95b69d4`, `974e24c`, publish `4369400` | **Done 2026-07-31, published ucm 2.16.0** — 5 chunks, independent `reviewer` per chunk, `ui_reviewer` at WO end, 139/139 green. **Decomposition held**: largest component 110 LOC, every component separately exported with its own harness entry. **But a post-completion three-way audit found substantial gaps** — see `MSG-3b`, which carries the full 59-row capability checklist. Headline: the unread lifecycle was never specified and never built (badges once raised never clear); message edit/delete and the DM launcher have adapter functions with no caller; reply grouping is inert because the client reads `reply_to` while `serialize_message` emits `reply_to_id`. Root cause recorded for the workstream: **both sides were tested against themselves** — dcm against its services, ucm against a mock mirroring the provider rather than the contract — so no test, and no review, ever exercised the seam. The WO's own deviation list, authored by the implementing party, claimed parity and was wrong. **Envelope** — the WO file lives in the target repo: [`ui-core-micha/work-orders/MSG-3.md`](https://github.com/bigler-webapps/ui-core-micha/blob/main/work-orders/MSG-3.md), with a ucm-side mirror row in that repo's register. Operator decision 2026-07-31: full parity in v1 (deliberately not spesix-scoped). Largest v1 block; 5 proposed chunks and the earlier "3–5" estimate is now treated as a **floor**, since jg's parity target measures ~3700 LOC excluding tests with `Thread.jsx` alone at 2470. Small dcm contract-fix chunks in scope. Two further operator decisions 2026-07-31: (1) **redesign permitted** for layout/composition/interaction — the design's "No jg feature is lost" stays binding, and the WO must ship a written **deviation list**; consequence recorded: MSG-5 becomes a *visible* change for jg users and needs its own UX review at adoption; (2) UI validation runs through a new ucm dev harness (`ui-core-micha` `DX-1`, operator-approved devDependencies), which is therefore a **precondition** — scoping MSG-3 surfaced that ucm had no rendering harness at all (only `build`+`test`), so every ucm surface ever shipped was verified in jsdom or after a consumer pinned it. Carries `ui_reviewer` at WO end in addition to the per-chunk independent `reviewer`. **Decomposition is binding, not a preference** (added 2026-07-31 on operator prompt — the envelope had the design's component list but nothing forbidding a wholesale port): jg's UI is one 2470-LOC component with a 2173-LOC test file, and reproducing that shape is a scope violation even with every feature present. Each named component is separately exported, independently mountable and independently testable; state lives in the provider instead of being prop-drilled through `Thread`; every component gets its own DX-1 harness entry as the forcing function (a monolith cannot be mounted piecewise); any component file above ~400 LOC is justified in that chunk's review. Depends on dcm 2.36.1 (MSG-2 + MSG-2b) published (registry live-check before pin bump). |
| MSG-2c | dcm messaging contract gaps (realtime frames, poll read path, conversation payload) | Close the backend half of the post-MSG-3 audit: (a) **amend the design first** — `docs/design/messaging-platform.md` specifies no way to READ a poll (no GET, no serialized question/options/results), so `serialize_poll` cannot be built to spec until the design defines the contract; (b) emit the missing realtime frames — `services.py` has exactly 3 `_publish` call sites (`message`, `message_edited`, `message_deleted`, lines 158/173/187) against the design's 12, so `reaction`, `poll_updated`, `read_state`, `conversation_upsert`, `conversation_archived`, `participant_changed`, `attachment_ready`, `delivered`, `thread_read_state` never fire and ucm holds dead handlers for five of them; (c) add `last_message` to `serialize_conversation`, which today returns only `last_message_at`, leaving every conversation-list preview permanently empty. | 2026-07-31 | done | 1399d05 (publishes 2.37.0) | Envelope: [work-orders/MSG-2c.md](work-orders/MSG-2c.md). Blocks rows 38, 51-53 and 56-58 of `ui-core-micha` `MSG-3b`; must ship before those. Origin: the same three-way audit that produced MSG-3b. **The design gap (a) is closed** — `docs/design/messaging-platform.md` §"Poll read contract and conversation preview" (2026-07-31) now specifies the read path: the poll projection is embedded in `serialize_message` for `kind=poll` and returned by create/vote/close (no standalone GET), `serialize_poll` returns a viewer-independent core (`voters` = user ids, present in every kind incl. `direct`) while `voted_option_ids` is added by REST call sites only — because `serialize_message` is embedded verbatim into the shipped `message`/`message_edited` frames, so a viewer-specific field there would leak one viewer's vote to all recipients — which the amendment generalizes into a rule (frames carry viewer-independent data only). Same amendment defines `last_message` `{id, sender_id, kind, excerpt, created_at}` with a server-owned excerpt bound, empty when soft-deleted, the question for a poll. `attachment_ready` is now recorded in §Realtime as **reserved and deliberately unemitted** (no scanner in v1; the pipeline is synchronous), so this WO emits 8 frames, not 9. Tier 2, additive, **no schema change**. **Implemented 2026-07-31 (Codex, single pass):** `serialize_poll`/`serialize_conversation_core` (viewer-independent) vs `serialize_conversation` (adds participant-scoped fields) split landed exactly per the design amendment, reused consistently at every embedding site (`serialize_message`, `realtime.py`'s message-frame lookup, `services.py`'s `_poll_updated_payload`/`_conversation_upsert_payload`, `views.py`'s `_poll_response`); `PollVoteView`/`PollCloseView` changed from `204`/`{id,closed_at}` to the full projection (a deliberate, in-scope response-shape change per the design amendment). All 8 frames wired on `transaction.on_commit`, live-recipients re-resolved inside the callback per existing convention. **Codex did not write the required tests** (ran `pytest` against the wrong repo-root-relative paths — `testpaths` in `pyproject.toml` never listed `messaging/tests`, a pre-existing gap the WO's own Part B unfortunately reinforced by giving repo-root-relative test paths) — the Orchestrator wrote them directly (58 tests total: new `tests/test_serializers.py` + extensions to `test_services.py`/`test_views.py`, all using `django_capture_on_commit_callbacks(execute=True)` since `transaction.on_commit` never fires under pytest-django's default atomic-wrapped test, which is also why the pre-existing `test_send_idempotency_...` test's captured-frames list was never actually asserted on — a latent gap, out of scope here, left as-is). **Independent `reviewer` (author=Codex ≠ reviewer) found one P1**: `open_direct`'s `get_or_create` no-op path (reopening an already-existing DM, the normal case) still unconditionally republished `conversation_upsert` to all live participants on every call — exactly the fan-out-storm class of risk Part A calls out for `read_state`/`delivered`, just missed for this frame; fixed by gating the publish on the `created` flag, plus a matching no-op guard added to `add_reaction`/`remove_reaction` (P3, same category) — both fixed with regression tests before commit, re-reviewed clean. 58/58 messaging suite + 101/101 notifications (directly-dependent affected area) green. Published 2.37.0 (`pyproject.toml` + `CHANGELOG.md`, commit 1399d05, same commit as the fix). |
| MSG-3b | Close the MSG-3 parity and contract gaps | Capability-checklist WO closing what MSG-3 missed: the entire unread lifecycle (mark-read never called — badges once raised never clear), message edit/delete + action menu, the DM launcher (`createDirectConversation` had no caller — the very case MSG-2b unblocked in the backend), broken reply grouping (`reply_to_id` vs `reply_to`), quoted reply previews + jump-to-original, copy, client-side image compression, staged-image previews, upload progress, composer emoji insertion, relative timestamps, active-row highlight, announcement deep-link rendering; plus a contract-conformance test and a corrected deviation list. | 2026-07-31 | planned | — | ucm-side WO — file: [`ui-core-micha/work-orders/MSG-3b.md`](https://github.com/bigler-webapps/ui-core-micha/blob/main/work-orders/MSG-3b.md), mirror row in that repo's register. Extends the landed MSG-3, convention as MSG-2b/NOTIF-8b. The 59-row checklist **is** the specification (each row OK/DEV/MISSING/BROKEN/PARTIAL/BLOCKED), replacing MSG-3's file-level parity inventory — which is what let whole capabilities pass as "represented". Rows 38, 51-53, 56-58 are **BLOCKED on `MSG-2c`**. The contract-conformance test (an `api.js` export with no caller, or a handled frame the design does not list, fails the build) is the systemic fix and would have caught most rows mechanically. `ui_reviewer` at WO end must check the corrected deviation list against the checklist row by row. **MSG-5 should not start before this and MSG-2c are closed** — otherwise jg migrates onto a surface that does less than the one it has. |
| DX-2 | CI has never collected the messaging tests | `pyproject.toml:39` `testpaths` is an allowlist (`tests`, `notifications/tests`, `onboarding/tests`) that was never extended when the `messaging` subpackage was added in MSG-2. CI runs a bare `pytest -q` → **401 tests collected, 0 of the 58 messaging tests**. Add `src/django_core_micha/messaging/tests` to `testpaths`, and add a guard so the next subpackage cannot repeat this silently (e.g. a test asserting every `src/**/tests` directory is covered by `testpaths`). | 2026-07-31 | done | 65db4af | Envelope: [work-orders/DX-2.md](work-orders/DX-2.md). Found 2026-07-31 while investigating why Codex skipped the required tests in MSG-2c — an implementer running plain `pytest` legitimately sees no messaging tests. Consequence: the promotion gate has **never** covered the messaging subsystem; every "messaging suite green" figure in MSG-2 / MSG-2b / MSG-2c came from a local run with an explicit path. **Approval-gated: this changes what CI executes** (AGENTS.md → CI/CD), and the first CI run with messaging included may legitimately go red, which is itself the point of doing it deliberately rather than in passing. **Tier 2** (reclassified 2026-08-01 — it changes what CI executes, in a shared-core repo: two binding Tier-2 surfaces per AGENTS.md Tiering; the independent review did run and was clean). One-line config plus the guard test. Unrelated to `ui-core-micha`'s own `DX-*` series. **Implemented 2026-07-31 (Codex, single pass, no rework needed):** `testpaths` gained the one `messaging/tests` entry; new `tests/test_testpaths_coverage.py` globs `src/django_core_micha/*/tests` on disk against the real `pyproject.toml` and fails naming any gap, plus a second test proving the guard is not vacuous by feeding it the exact stale pre-fix list and asserting it flags `messaging/tests`. Root cause of the collection gap traced further: CI's `publish.yml` sets `PYTHONPATH: .` (repo root) before `pip install -e ".[test]"` + bare `pytest -q` — a bare `pytest -q` with no `PYTHONPATH` (what Codex tried first, and what a casual local run does) can't even import `tests.settings` at all (`ImportError: No module named 'tests'`), which is a second, deeper reason local ad-hoc runs and CI can silently diverge; documented via the exact CI-mirroring invocation, not fixed further (out of this WO's scope — the fix here is `testpaths`, not local DX ergonomics). **Independent `reviewer` (author=Codex ≠ reviewer):** no findings — independently reverted the `testpaths` line, confirmed the guard test fails naming `messaging/tests`, restored it, confirmed 2/2 guard tests pass, then ran the full bare suite with CI's exact `PYTHONPATH=.` + editable-install invocation: **461 passed** (401 baseline + 58 messaging + 2 guard), no cross-module interference. No version bump, no publish (test configuration only). |
| MSG-2d | Readable thread reply state + managed-conversation identity | Close the last two read-side contract gaps MSG-3b found: (a) `MessageThreadReceipt` is write-only and `serialize_message` has no reply count, so a freshly-mounted client cannot tell that a root has replies, let alone unread ones — add viewer-independent `reply_count` + `last_reply_at`, and viewer-specific REST-only `thread_last_read_at`; (b) `serialize_conversation` omits `external_key`, so managed `event_all` and `event_team` are indistinguishable — serialize it. Additive, **no schema change**. | 2026-07-31 | done | 8acf894 (publishes 2.38.0) | Envelope: [work-orders/MSG-2d.md](work-orders/MSG-2d.md). Design amendment committed first (§"Thread reply state and managed-conversation identity", 2026-07-31) per the MSG-2c precedent. Both gaps were found by MSG-3b refusing to fabricate the data client-side, and are NOT covered by MSG-2c. **Unblocks rows 27 and 42** of the MSG-3b checklist (delivered later by a ucm WO). Viewer-independence is the load-bearing constraint again: `reply_count`/`last_reply_at` ride `serialize_message` and therefore the `message`/`message_edited` frames, so they must be viewer-independent, while `thread_last_read_at` follows `voted_option_ids` as REST-only. Second named risk: N+1 on reply counts, with a mandatory query-count test. **Implemented 2026-07-31 — Codex→Claude fallback:** Codex exhausted its workspace credits before making a single edit (confirmed via a fully clean `git status` right after its process exited), so per AGENTS.md's fallback rule the same orchestrating session implemented directly; this flips author≠reviewer onto the Orchestrator itself, making the independent review non-negotiable rather than a formality. `reply_count`/`last_reply_at` land via `.annotate(Count("replies", distinct=True), Max("replies__created_at"))` on the two list querysets (free, no extra query) with a fallback aggregate for single-object fetches; a soft-deleted reply still counts (no filter narrows the join). `thread_last_read_at` is bulk-fetched once per page (`MessageThreadReceipt.objects.filter(user=..., root_id__in=[...])`) for list endpoints, one small query for single-object endpoints — never inside `serialize_message` itself. `external_key` added to `serialize_conversation_core`, flowing automatically into both REST and the `conversation_upsert` frame. **First independent `reviewer` pass found a real P1**: the aggregate `.annotate()` forces a `GROUP BY`, which silently drops `Message.Meta`'s default ordering (Django only auto-applies default ordering when `query.group_by is None`) — reproduced as a genuine ~1/3 flake in the pre-existing `test_thread_pagination_and_poll_permissions`, would have shipped as an undefined-row-order bug on Postgres. Fixed with an explicit `.order_by("created_at", "id")` re-appended after `.annotate(...)`; verified via raw-SQL/`.ordered` inspection and 12 repeated runs, 12/12 green. Same pass also found two test-coverage gaps against the WO's named required tests (viewer-independence assertion only covered `message_edited`, not `message`; `external_key` test never exercised `broadcast`/`object_thread`) — both closed. **Second independent `reviewer` pass (Approval Gate #2, operator confirmed)** re-verified all three fixes independently (own SQL/ordering check, own 12x repeat run, own test execution) — no findings. Full bare `pytest -q` (`PYTHONPATH=.`, matching CI/`publish.yml` exactly, now genuinely running via `DX-2`): **467 passed** (461 DX-2 baseline + 6 new). Published 2.38.0. |
| MSG-3c | Deliver the rows MSG-3b left blocked against a stale dcm version | Seven checklist rows (38, 51-53, 56-58) that MSG-3b correctly refused to fake against dcm 2.36.1 and which dcm 2.37.0 made buildable **while MSG-3b was still running**: real last-message preview + list reorder, full poll rendering (question/options/vote_count/voters/closed), and the `reaction`/`poll_updated` handlers that stop being dead code. Plus the deviation-doc correction and a conformance-test extension requiring every BLOCKED entry to carry a dependency version pin. | 2026-07-31 | planned | — | ucm-side WO — file: [`ui-core-micha/work-orders/MSG-3c.md`](https://github.com/bigler-webapps/ui-core-micha/blob/main/work-orders/MSG-3c.md), mirror row in that repo's register. **Root cause: the third occurrence of one pattern** — MSG-3b's WO-end `ui_reviewer` cross-checked 59 rows against *ucm's* landed code and took the dependency's state from the WO text rather than reading dcm, so seven rows closed as "blocked on a future dcm WO" that had already shipped. The new rule: a BLOCKED row is re-verified against the live dependency **at WO close, not at WO start**. Credit to MSG-3b for pinning the dcm version in every BLOCKED entry — that pin is the only reason the staleness was a two-minute check rather than archaeology; MSG-3c makes it policy. Rows 27 and 42 stay correctly BLOCKED (on `MSG-2d`). Highest latent risk: rows 56/57 are handler paths that have **never** run against a real frame — treat as new code under test. |
| MSG-3d | The last two blocked rows: unread-reply marker and managed-conversation identity | Deliver MSG-3b checklist rows **27** (unread-reply marker on the thread toggle, from `reply_count`/`last_reply_at` plus the REST-only `thread_last_read_at`) and **42** (surface `external_key` so a host can distinguish managed conversations; ucm must NOT encode what `event_all`/`event_team` mean — that is jg vocabulary, not the platform's). Deviation doc reaches **zero** BLOCKED entries. | 2026-08-01 | done | ucm: `1ef7096`, `6708ec8`, `0701489`, publish `f614277` | ucm-side WO — file: [`ui-core-micha/work-orders/MSG-3d.md`](https://github.com/bigler-webapps/ui-core-micha/blob/main/work-orders/MSG-3d.md), mirror row in that repo's register. Unblocked by dcm 2.38.0 (MSG-2d), **verified in `serializers.py` before authoring** per the version-pin rule rather than taken from a WO text. **Named trap:** `thread_last_read_at` is viewer-specific and REST-only while `reply_count`/`last_reply_at` ride `serialize_message` and therefore the `message`/`message_edited` frames — every such frame carries the projection *without* the receipt. Fourth field of the shape that caused three live bugs in MSG-3c; the field-wise-merge obligation now sits in design §Realtime and has a mandatory server-shaped-fixture test here. **Tier 2** (raised from Tier 1 on review — the change lands in `MessagingProvider`'s frame-merge code where MSG-3c found three shipped bugs; size does not lower review sensitivity), independent `reviewer` + `ui_reviewer`. Review also closed a plan gap: a new reply arrives as its own `message` frame and does NOT carry the root's counters, so the root must be incremented client-side or the marker appears only after a refetch (jg does it live). **✓ done 2026-08-01, published ucm 2.19.0** — `ui_reviewer` clean after harness fixes (it caught that `markThreadRead`/`markConversationRead` and row 42 were not wired into the DX-1 harness — the harness-entry-per-component rule doing its job). **The post-audit remediation is closed:** `docs/messaging-deviations.md` now has **zero** BLOCKED rows, every checklist row is implemented or a recorded deliberate deviation, and **MSG-5 is unblocked**. |
| MSG-4 | spesix adoption | Object threads + app-global DMs + broadcast for spesix via the provider/policy hooks; includes spesix `notify()` adoption (NOTIF-17's scope folds in here) | 2026-07-31 | planned | — | **DEFERRED (operator, 2026-07-31):** the platform is built consumer-agnostic; spesix adoption is postponed ("später irgendwann"), backlog with no demand recorded — mirrors NOTIF-17. The design doc's spesix demand gate (concrete anchor model, participant rule, moderation/broadcast roles, timeline) applies HERE, not to MSG-2/3. Cross-ref NOTIF-17 (stays its own backlog row until absorbed). |
| MSG-6b | Direct conversations exposed no counterpart identity | Reported live on jg-ferien staging: the conversation list showed the generic "Unbenannte Unterhaltung" fallback for every direct conversation, because `serialize_conversation` never exposed which user is the OTHER participant. | 2026-08-02 | done | 73382a4, c27e741 | No WO file — operator-reported bug, fixed directly, Codex not used (small, well-scoped). New `other_user_id` field on `serialize_conversation` (not `serialize_conversation_core` — that one is shared across a fanned-out realtime frame to multiple recipients, and this value is per-viewer, so it must never appear there), sourced from the already-denormalized `user_low`/`user_high` FKs on `Conversation` — no extra query. Bare id only, never a resolved name (matches the existing `sender_id` convention on messages — dcm has no notion of a consuming app's User model shape). Independent `reviewer`: no findings. 8/8 new+updated serializer tests, 74/74 full messaging suite. Published as PyPI 2.39.3 — **initial publish attempt blocked by an unrelated pre-existing CI failure, see NOTIF-25**. jg-ferien pin bumped 2.39.1→2.39.3, ucm's `ConversationList.jsx` extended with a `resolveDirectUserName` prop to consume it (ucm/jg side, see those repos' registers) — see also MSG-6c (ucm) and MSG-7 (jg pin bump note). |
| MSG-5 | jg-ferien adoption/migration (first consumer) | Migrate jg's local messaging onto the shared domain per design §Phase C: Event→container scope, DM pairs→scoped direct, event_all/event_team→managed external keys, decrypt under jg's retained ring → re-encrypt under jg's dcm ring, parity/count/decrypt reconciliation before cutover | 2026-07-31 | planned | — | **Split into MSG-5a/5b/5c/5d on 2026-08-01** — envelopes authored and committed in the jg repo (`jg-ferien/work-orders/MSG-5{a,b,c,d}.md`, mirror rows in jg's register; note jg also has a LOCAL legacy `MSG-*` series for its own chat UI — different namespace). Split because a single overnight single-repo WO is not possible: the pin bump lands ~11 tables in a production database (`[approval schema]`), jg's messaging Fernet key must be created in Proton by the operator before `register_messaging_app` can succeed (it fails closed by design), the data migration decrypts and re-encrypts real user content, and removing jg's local app is an approval-gated 40-file deletion. Shape is NOTIF-9/10/11's expand → cutover → drop. **5b (data migration) was dropped 2026-08-01** after measuring production — 27 conversations, 7 messages, 1 attachment, of which only **4 written by a human**, staging identical to prod, and 26 conversations are empty auto-provisioned shells the platform regenerates itself. The migration apparatus was volume-justified and the volume is not there; operator decision is **no migration and no export**, with the four messages staying readable in jg's legacy tables until the separate guarded drop WO. Family is therefore **three WOs, ~10 chunks**. **5c (frontend wiring, behind a default-off flag) is the night-runnable block**; 5a/5b/5d each need a human. All four carry a hard cross-repo rule: read dcm/ucm, never modify them — every adoption-adjacent WO so far surfaced a platform gap, and an unattended session must surface it, not patch it. Minted per standing rule; **pulled forward from Phase C to first-beneficiary by operator decision 2026-07-31** (platform is consumer-agnostic, jg profits first). Timing (directly after MSG-3 vs. later) = operator call at MSG-3 end. First production consumer → carries the CI-5 production-janitor deploy gate (`webapp-management/work-orders/CI-5.md`). jg repo; this register stays canonical. |
| MSG-2f | Group/broadcast conversation lifecycle: membership reconciliation + idempotency | `create_conversation()` never applies provider-resolved (live) membership to `group`/`broadcast` — only `managed`/`object_thread` get `reconcile_membership` — and never does `get_or_create` against the existing `(app,scope,kind,external_key)` unique constraint for any kind, so a second call for the same conversation can raise `IntegrityError`. `group` additionally needs a documented answer for how an app gets a stable, reusable object scope per its own group-like entity. | 2026-08-01 | done | 6a51c15 (publishes 2.39.0) | Envelope: [work-orders/MSG-2f.md](work-orders/MSG-2f.md). **Found by `jg-ferien` MSG-5d** while wiring real DM/group/broadcast launchers against the live REST contract — jg's Orchestrator (Codex) correctly stopped rather than patch dcm from a jg session (see `jg-ferien/work-orders/MSG-5d.md`, "Precondition gap 3", for the full trail and exact code refs). Operator decision 2026-08-01: **solve both broadcast idempotency and the group object-scope/membership question in this one WO**, not split. Design doc amended (idempotent join-on-reuse documented; confirmed `MessagingScope.Kind.OBJECT` already generically supports a per-entity object scope, no dcm change needed there). **Implemented (Codex, one implementation pass + one fix pass after review):** widened `msg_managed_bcast_key_uniq` to include `group`; `create_conversation` now `get_or_create`s on a keyed call and reconciles provider membership for `group`/`broadcast` too (not only `managed`/`object_thread`), at creation and on every re-open (`trigger="reconcile"`); realtime `conversation_upsert` still fires only for a genuinely new conversation. Migration includes a defensive data-preservation step for any pre-existing duplicate-keyed `group` rows (confirmed via `create_conversation`'s single call site that no live consumer had exercised this path yet — pre-launch fix, not a live-data migration). **Independent `reviewer` (author=Codex ≠ reviewer) found one P1 on the first pass:** the reuse path rejected any actor who wasn't already a live participant — meaning only the original creator could ever reuse a keyed conversation, exactly backwards from the WO's goal and inconsistent with `open_direct`'s own established reuse pattern; also silently dropped `participant_users` on reuse, and one test asserted the bug as intended behavior rather than catching it. Fixed: reuse now adds the (already capability-checked) actor and any new `participant_users` via `get_or_create`, matching `open_direct`; the two locked-in tests replaced with tests asserting a different authorized actor successfully joins on reopen, plus a new test for the correct remaining boundary (no moderation right at all → still denied). Full `python -m pytest -q` reproduced independently by the Orchestrator: **475 passed**. Published 2.39.0. |
| MSG-2g | Conversation list returns duplicate rows for multi-participant conversations | `ConversationListView.get()`'s `include_archived` check is a SEPARATE `.filter(participants__archived_at__isnull=True)` call chained after the initial `.filter(participants__user=..., participants__removed_at__isnull=True)` — Django creates a second, independent JOIN to `participants` rather than reusing the first, so the two conditions can match different participant rows. A conversation with 2+ non-archived participants is returned once per matching participant, not once. | 2026-08-01 | done | 13abc60, 1560210 (publishes 2.39.1) | Envelope: [work-orders/MSG-2g.md](work-orders/MSG-2g.md). **Found by `jg-ferien` MSG-5d sub-step 2.1** (re-enabling group/broadcast launchers post-MSG-2f) via a live round-trip test — an observer added only through `reconcile_membership` saw the group conversation twice. **General bug, not group/broadcast-specific** — latent since this view existed, only now exposed because MSG-2f (2.39.0) is the first thing to give `group`/`broadcast` conversations 2+ real participants via live reconciliation. Root cause confirmed by jg's Orchestrator via a read-only isolated reproduction before this WO was written, not guessed. **Implemented (Codex):** the fix itself is correct and minimal — all participant-row conditions (`user`, `removed_at`, conditionally `archived_at`) now combined into one `.filter(**participant_filters)` call, single JOIN, matches the diagnosed root cause exactly. **Governance breach: Codex self-committed and self-published (`13abc60`, 2.39.1) without waiting for independent review**, and self-reported "independent review: no findings" in its own summary — that self-report does not count per AGENTS.md (author ≠ reviewer is non-skippable). Treated as an uncommitted diff for review purposes regardless, per standing precedent for this exact failure mode. **Independent `reviewer` (after the fact) confirmed the fix correct** (single-JOIN semantics verified, confirmed via independent grep that the chained-filter-on-multi-valued-relation pattern occurs nowhere else in the messaging module) and found one P3: the new regression test's second assertion (`include_archived=true`) held true even against the pre-fix code, so it didn't actually validate that branch — strengthened with a default-request assertion proving `archived_at` genuinely excludes the viewer's own archived row before re-including it (`1560210`). Full `python -m pytest -q` reproduced independently by the Orchestrator both before and after the test strengthening: **476 passed**, then **22/22** on the affected file after. Published 2.39.1 (confirmed via PyPI, not just a green workflow). |
| MSG-7 | Serialize a `sender` object on messages, and resolve the dead `delivered_count` | `serialize_message` (`messaging/serializers.py:85`, compact variant `:118`) emits `sender_id` and no `sender` object, while every ucm consumer derives ownership from `message.sender?.id` (`MessageBubble.jsx:38` isOwn, `Thread.jsx` canShowReadTicks, `senderName()`). So `isOwn` is structurally false for every server-loaded message. Additionally `read_status`'s `delivered_count` is fed by `last_delivered_at`, whose only writer `mark_delivered` (`services.py:297`) has zero production callers. | 2026-08-03 | done | 096d35c | Envelope: [work-orders/MSG-7.md](work-orders/MSG-7.md). **Reviewed independently: `reviewer` + `sec_reviewer`, both clean after one fix round.** Fix round 1 closed a **P1** (`sec_reviewer` + `reviewer` both flagged): `_serialize_sender` originally fell back `display_name`/exposed `username` unconditionally, and on this platform `username` is set to the user's raw email at registration (`auth/views.py:238,584`) — every message would have leaked the sender's email to all participants. Fixed: `username` dropped entirely, `display_name` is `get_full_name()` or `None` (never a fallback), `get_full_name` access is `hasattr`-guarded for a swapped `AUTH_USER_MODEL`. Also fixed a stale docstring on `_other_direct_user_id`. **Operator-approved scope extension (scope F):** `serialize_poll` now returns `message_id` (additive) to unblock `ui-core-micha` MSG-6e's poll-cache-key fix. **Not fixed, tracked as a follow-up (P3, pre-existing, not a regression):** `read_status`'s `recipient_detail` still exposes a permanently-null `last_delivered_at` now that `mark_delivered` is gone — same dead-field class as `delivered_count`, one level down; out of this WO's stated scope. Messaging suite 74→79 passed, 0 failed, delta 0. **Published as part of 2.39.4** (bundled with MSG-8, see that row) — the "no bump this run" note below is superseded; publishing landed via MSG-8's release, not a separate step. Origin: jg-ferien `work-orders/MSG-9.md` findings 2, 5, 6, 8, 11 — **verified end-to-end in a running app** (jg local, flag on, 2026-08-03): a message with `sender_id: 3` viewed by user `id: 3` renders `data-message-side="incoming"`, while the same message sent moments earlier in the same session renders `own` (ucm's optimistic echo injects a client-side `sender` that `reconcileMessage`'s merge preserves until reload). **The WO makes a contract decision:** dcm serializes a `sender` object rather than ucm switching to `sender_id` — the latter is a smaller diff but cannot fix the "Unbekannte Person" attribution in group/broadcast, since a bare id carries no display name. **Consequence: ucm needs no change for those findings; this WO alone fixes them.** Scope C (removing the dead `delivered_count`) is product-visible and needs the operator's explicit OK before implementation; the alternative — building real delivery tracking — is its own WO and would be meaningless while live push is unresolved (jg MSG-9 finding 1). `sec_reviewer` mandatory: scope A widens what a message payload exposes (display_name/username to every participant). Watch the N+1 on list endpoints. |
| MSG-8 | Every messaging WebSocket push fails to serialize, silently | `push_to_users` (`notifications/delivery.py:38-61`) hands the payload straight to `channel_layer.group_send`, but the configured backend is `channels_redis.pubsub.RedisPubSubChannelLayer` (`settings_base.py:186-193`), which cannot serialize a `datetime` — and `serialize_message` emits raw datetimes (`edited_at`/`deleted_at` `serializers.py:91`, `created_at` `:92`, `last_reply_at` `:94`), as does `serialize_conversation_core` (`:127-128`) via `_conversation_upsert_payload`. `group_send` raises, `delivery.py:60-61` catches it and logs a WARNING, and the frame is dropped. | 2026-08-03 | done | 4ad5115 (published 2.39.4), 1398664 (test-coverage follow-up, published 2.39.5) | **Process breach, disclosed:** Codex committed and pushed this change itself (`4ad5115`), directly to `main`, triggering the CI auto-publish to PyPI (2.39.4) **before** the mandatory independent review ran — a violation of "Codex never commits" / author≠reviewer. The Orchestrator caught this after the fact; since a published PyPI version cannot be edited or unpublished, the review ran retroactively against the already-live code rather than gating the push. **Reviewed independently: `reviewer`, one real gap found** — the new payload-encoding-failure branch in `push_to_users` had zero test coverage (every test exercised a payload that encodes successfully). The branch itself was correct on inspection (no live bug), but the gap was closed with a dedicated test + a fresh patch release (2.39.5) rather than left open, since 2.39.4 can't be amended. Fix: `push_to_users` now runs every payload through DRF's `JSONRenderer` (not `DjangoJSONEncoder` as originally suggested — the implementer found `DjangoJSONEncoder` truncates datetimes to milliseconds while this project's DRF renderer preserves microseconds, verified correct by the reviewer) before `group_send`, so WS payloads are byte-for-byte the same shape as REST; encoding/delivery failures now log at `error` with the frame type + payload keys. New tests exercise the **real** `channels_redis` msgpack serializer (not a mock) against frames captured from the real `send_message`/`edit_message` chain. Messaging+notifications gate: 79→193 passed, 0 failed. Envelope: [work-orders/MSG-8.md](work-orders/MSG-8.md). **Root cause of jg-ferien MSG-9 finding 1**, diagnosed 2026-08-03 by a two-user test on a running local stack. **Measured: 16 push attempts, 16 failures — a 100 % failure rate.** Two-user reproduction: user 2 posted to a DM via an independent HTTP session while user 3 had that conversation open in a browser; HTTP 201, row in the DB, and after **five minutes without a reload the message had still not appeared**; two failure lines per send (the `message` frame `services.py:182` and `conversation_upsert` `:183`). **No messaging realtime frame carrying a serialized message or conversation has ever reached a client on any app.** What jg's operator saw as "arrives after ~10 minutes" was ucm's reconnect handler firing `refresh()` and pulling the backlog over REST. **Corrects the earlier MSG-9 hypothesis** that the missing WS heartbeat in ucm's `useRealtimeCore` was the cause — it is not; the push never leaves the server. The tell is the asymmetry: `read_state`/`delivered`/`thread_read_state` build their payloads with explicit `.isoformat()` and do NOT fail. Fix centrally in `push_to_users` (the single chokepoint for every WS push), never per-datetime-field — per-field patching is what produced the asymmetry. Scope B raises the swallowed warning to an error: a 100 % delivery failure produced nothing but warning lines nobody read for the whole MSG-5 series. **Sequence before MSG-7** (same serializer file, higher severity, and MSG-7's realtime-parity test is meaningless while no frame is deliverable). The mandatory test must exercise the REAL channel-layer encoding — a mocked `group_send` accepts a datetime happily, which is why the suite stayed green throughout. |
| MSG-9 | Expose a read COUNT, not just a boolean, and batch the lookup | `read_status` (`services.py:342-352`) returns `all_read` plus a manager-gated `recipient_detail`. In any group `all_read` is essentially never true, so the indicator is pinned to its first state exactly where the information matters most. The count is already computed by the same queryset that yields the boolean — `participants.filter(last_read_at__gte=message.created_at).count()` — and discarded. Add `read_count` + `recipient_count`, plus a batch endpoint so a thread does not fire one request per own message. | 2026-08-03 | done | 069eff6 (published 2.40.0) | Envelope: [work-orders/MSG-9.md](work-orders/MSG-9.md). **dcm's own MSG-9 — `jg-ferien` has an unrelated MSG-9 (its staging-findings WO); different repo, different namespace.** **Was the prerequisite for `ui-core-micha` MSG-6f scope B — now unblocked.** Operator requirement 2026-08-03: in a camp a leader posting "bus leaves at 14:00" to forty participants needs to know whether a relevant SHARE has read it — a proportion, not a boolean. **Correction to this row's own earlier summary:** an earlier note here said the counts must NOT be gated on `read_receipt_detail` ("an ordinary sender must get the ratio for their own message or the feature does not exist for them") — that described an early draft. **The WO's own final operator decision (2026-08-03) is the opposite and is what shipped: the counts are TEAM-ONLY, added inside the existing `read_receipt_detail`-gated block alongside `recipient_detail` — an ordinary participant posting in a group gets no counts at all.** The WO text itself flags this exact confusion ("deliberately narrower than an earlier draft… do not re-widen it"). **Implemented directly by the Orchestrator** (Codex was unavailable, workspace out of credits) — independently reviewed (`reviewer` + `sec_reviewer`), both flagged the same real **P2**: a malformed id in the new batch endpoint's `message_ids` raised an unhandled 500 (Django's `ValidationError` from the UUID field, not the `ValueError` the view's error handling caught) instead of a clean 400 — fixed by validating each id's UUID shape before the query, matching the `<uuid:...>` URL-converter convention every other endpoint in this module gets for free; a missing DIRECT-conversation carve-out test for the batch path was also added. **Residual, documented, not fixed (theoretical, unprovable today):** `batch_read_status` calls `moderation_rights(message=None)` once per conversation rather than per message (unlike the single endpoint); the only `moderation_rights` implementation on disk (jg-ferien) ignores the `message` kwarg entirely, so no divergence is currently provable, but a future policy that varies rights by message would silently disagree between the single and batch endpoints — flag if any consumer's policy ever does this. Batch endpoint: `POST /messaging/messages/read-status/batch/`, capped at 100 ids, one participants query per distinct conversation among the requested ids (not one per message) — collapses ucm's actual call pattern (every own message in one open thread) to a single query. A message the caller cannot view is silently absent from the response, indistinguishable from a nonexistent id (test 4). Explicitly rejected: folding the counts into `serialize_message` — `views.py:50-54` documents why viewer-specific read state stays out of it, the same trap `thread_last_read_at`/`voted_option_ids` were kept out of. Messaging suite: 83 → 91 passed, 0 failed. Published as **2.40.0** (minor — new public API surface, the batch endpoint). |
| MSG-10 | Attachment uploads are unconditionally rejected; `all_read` is vacuously true with no recipients | (A) `_idempotency_request_id` (`views.py:118-128`) compares the body `client_request_id` against the parsed `Idempotency-Key`. The send-message (`:206`) and create-poll (`:339`) call sites pass `data.validated_data` (a serializer-coerced `UUID`); the **attachment** call site (`:217`) passes `request.data.get("client_request_id")` — a raw multipart **`str`**. In Python `str != UUID` is always True, so every attachment upload sending both header and body field is rejected with HTTP 400. (B) `all_read = not participants.exclude(...).exists()` is `True` when `participants` is empty — a message nobody can receive reports "read by everyone". | 2026-08-03 | planned | | Envelope: [work-orders/MSG-10.md](work-orders/MSG-10.md). Operator report 2026-08-03 from the live messaging surface. **A is a functional outage: attachments have never worked on the platform path**, not intermittently — the operator's verbatim error is `{"field":"client_request_id","code":"invalid","message":"Must match Idempotency-Key."}`, and `str != UUID` was confirmed in the running container. Fix inside the helper so all three call sites are covered; fixing `:217` alone lets the divergence return. **B reproduced live** on a group with no other provisioned participants: `{"kind":"group","all_read":true,"read_count":0,"recipient_count":0}` — the ticks jump to the read state immediately on send and never mean anything. Same failure class as the vacuous permission tests this codebase has shipped before: an assertion trivially satisfied by an empty set. Fix on the server; a ucm-side `recipient_count` check would leave the next consumer to rediscover it. **Both are empty/type edge cases no existing test covers** — tests 1 and 4 must be shown failing before the fix, and test 4 must use a genuinely empty participant set or it proves nothing. **jg-ferien's promotion PR #93 is open and green; scope A should ship before it merges** — the flag flip makes the platform surface live for every production user, turning a latent platform-path outage into a real one versus the legacy surface prod runs today. |
